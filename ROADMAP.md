# Roadmap

Axon's development has proceeded in informal, numbered phases (see git history and inline comments for Phases 1–7: CS API core, federation foundation + state-res v2, E2EE sync, media/push/app-services, spaces/relations/room upgrades, delegated OIDC login, MSC3814 dehydrated devices). This document tracks what's planned from Phase 8 onward.

## Phase 8 — E2EE reliability & cross-server delivery (done)

Despite a substantial existing E2EE surface (key upload/query/claim, cross-signing, key backup, dehydrated devices), three relay-layer bugs made device verification and message delivery unreliable across clients and homeservers:

- `sendToDevice` never woke a long-polling `/sync` — a queued Megolm room-key share or SAS verification message sat undelivered until the recipient's client-chosen timeout happened to elapse on its own, not until the sender actually sent it. Every existing sync test used `timeout=0`, so this was invisible to the suite.
- `device_lists.changed` only fired on key upload/cross-signing, never on newly sharing a room — a client trusting that signal (rather than deriving key-query needs from room membership itself) would never learn to query/verify a fresh room-mate's devices. `device_lists.left` was hardcoded to `[]`.
- `sendToDevice` had no local/remote split, unlike `/keys/query` and `/keys/claim` — any to-device message aimed at a user on another homeserver was silently dropped forever, since there was no federation EDU handling at all (inbound or outbound).

Fixed by: broadcasting a PubSub wake on to-device delivery (mirroring the existing `device_list_updates` pattern); re-touching `device_list_updates` for both parties on room join and a new `device_list_partings` table + cursor for room leave; and adding `m.direct_to_device` EDU support (outbound via `AxonWeb.FederationFanout`, inbound via `FederationController.send_transaction/2`), plus wildcard (`"*"`) device-id expansion. Regression coverage in `apps/axon_web/test/e2ee_delivery_test.exs` exercises the real long-poll path with a nonzero timeout (closing the test-coverage gap that hid bug 1) and the cross-server EDU path via `AxonFederation.FakeRemoteMatrixServer`.

## Phase 9 — Federation EDU & real-time parity (done)

Full bidirectional EDU support (`m.typing`, `m.receipt`, `m.presence`), building on the `m.direct_to_device` scaffolding from Phase 8, plus durable outbound federation delivery.

- **Typing indicators** (`PUT /rooms/:room_id/typing/:user_id`) were a complete no-op stub — acknowledged and discarded. Now backed by `AxonSync.Typing` (in-memory, auto-expiring, mirroring `AxonSync.Presence`'s design), surfaced in `/sync`'s per-room `ephemeral` section, and federated as `m.typing` EDUs both ways.
- **Read receipts** had no PubSub wake at all (unlike to-device/device-lists/account-data, fixed in Phase 8) — a receipt posted to an otherwise-quiet room sat unseen until something else happened in that room, since incremental sync only included a room at all when it had a *new timeline event*. Fixed with a new `ephemeral_updates` log (mirroring `device_list_updates`) that both wakes long-pollers and makes a room eligible for inclusion on ephemeral-only changes; `m.read` receipts (never `m.read.private`) now federate as `m.receipt` EDUs.
- **Presence** now federates as `m.presence` EDUs to every server sharing a room with the user, but only on an actual state transition (online/unavailable/offline or status_msg change) — not on every `bump_activity` touch, which happens on every authenticated request and would otherwise flood federation peers.
- **Durable outbound delivery**: `AxonFederation.OutboundQueue` persists every outbound PDU/EDU transaction before the first attempt and retries with exponential backoff (30s → 1hr cap, giving up after 7 days) on failure, reusing the same row id as the txn_id on every retry so a remote server that already processed an earlier attempt responds idempotently. Replaces the previous fire-and-forget behavior in `AxonWeb.FederationFanout`, where a failed delivery just logged a warning and was dropped — meaning a remote server being briefly unreachable silently lost whatever was sent to it during that window. Matters here specifically because this deployment federates with other servers in production use, not just same-server accounts.

Regression coverage: `apps/axon_federation/test/outbound_queue_test.exs` (failure → persisted → retry → success), `apps/axon_web/test/phase9_ephemeral_test.exs` (typing/receipt local wake-up and room-inclusion, both EDU directions for typing/receipts/presence, permission checks, expiry).

## Phase 10 — Sliding Sync (MSC4186) (done)

`POST /_matrix/client/unstable/org.matrix.msc4186/sync` alongside classic `/sync` (kept for legacy clients). A pragmatic subset of the MSC rather than full conformance:

- **Lists**: `ranges`-paginated, recency-sorted (`sort` is always treated as `by_recency`) room lists over the user's *joined* rooms, with `is_dm`/`is_encrypted` filters. Every response re-sends a full `SYNC` op per range rather than diffing against a remembered window — no `conn_id` session state is kept, so this is spec-valid but less bandwidth-efficient than real add/remove diffing.
- **`room_subscriptions`**: explicit per-room overrides, included regardless of list ranges; merged with any list config referencing the same room (union of `required_state`, max `timeline_limit`).
- **`required_state` resolution**: concrete `[type, state_key]` pairs, `*` wildcards, `$ME` (substitutes the requesting user), and `$LAZY` (member events lazy-loaded to just the current timeline's senders, plus self) — reusing `EventStore.get_current_state/1`.
- **Extensions**: `to_device`, `e2ee` (OTK/fallback-key counts, `device_lists`), `account_data` (global + per visible room), `receipts` (`m.read` only), `typing` — all built from the same `AxonWeb.SyncHelpers` module classic `/sync` now delegates to as well, so the two endpoints can't drift apart on E2EE/device-list/ephemeral semantics (the extraction this required also served as a small refactor: those cursor/extension helpers used to be private duplicates baked into `SyncController`). Extension params aren't sticky across requests — a client must resend `"enabled": true` every time, not just once.
- **Known gaps, deliberately deferred**: invited/knocked/left rooms don't appear in `lists`/`room_subscriptions` yet (exposing them safely means re-deriving the same invite-state-only visibility rules classic sync's `build_invite_state/2` already enforces — deferred rather than done half-safely); `notification_count`/`highlight_count` are always 0 (this codebase doesn't compute per-room unread/highlight counts anywhere yet, including in classic `/sync` or push dispatch, so this isn't a regression).

Long-poll wake-up reuses `AxonSync.Manager.wait_for_events/3` unchanged, so the Phase 8 wake-up fixes (to-device, device-list, ephemeral) apply here too. Regression coverage: `apps/axon_web/test/sliding_sync_test.exs` (13 tests: list sort/ranges/filters, `room_subscriptions`, `required_state` incl. `$LAZY`/`$ME`, the long-poll wake path with a real nonzero timeout, and every extension).

## Phase 11 — E2EE/verification test hardening (done)

Two real bugs found and fixed while expanding coverage of the surface that's seen the most iterative bugfixing:

- **Stale test, not a stale bug**: `e2ee_multi_device_test.exs` asserted a first-time cross-signing `master_key` upload returns 401 requiring a UIA retry. MSC3967's `uia_exempt?/4` (`key_controller.ex`) correctly exempts first-time setup (nothing on file yet to protect) and has for a while — the test just never caught up. Fixed the assertion and added a real UIA-required case alongside it (rotating to an unrelated master key that isn't signed by the one on file), so the branch that *does* still require UIA has coverage too.
- **Key backup consistency**: `PUT /room_keys/keys` never validated its `version` query param against the user's actual current backup version — a client retrying against a stale, deleted, or superseded version would silently insert rows with no error, and `count`/`etag` on `GET /room_keys/version` would never reflect the write. Now returns `403 M_WRONG_ROOM_KEYS_VERSION` (with `current_version` in the body) per spec, covered by `apps/axon_web/test/key_backup_consistency_test.exs`.

New Complement-style scenario coverage in `apps/axon_web/test/e2e/verification_flow_test.exs`:

- A full `m.key.verification.*` SAS exchange (request → ready → start → key ×2 → mac ×2 → done) between two users' devices, with every hop driven through a real nonzero-timeout long-poll on both sides — not a single event checked with a short poll, which is all any prior test did.
- The same shape across two homeservers via `AxonFederation.FakeRemoteMatrixServer`, with a realistic nonzero timeout on the inbound EDU recipient's long-poll — the existing federation EDU test only short-polled after the fact, so it couldn't catch a wake-up regression on the federated path the way it already could locally.
- The wildcard `"*"` to-device target (`KeyStore.expand_wildcard_device/2`, added in Phase 8) had zero test coverage until now — verified against a 3-device account, confirming delivery to all three and non-delivery to an unrelated user.
- A cross-signing trust-chain scenario: bob signs alice's master key after "verifying" her; `/keys/query` shows bob his own signature but not an uninvolved third party, exercising the visibility rule end-to-end over HTTP rather than just at the `KeyStore` unit level.

## Phase 12 — Room version 12, third-party invites, guest access (done)

Turned out to be a much bigger lift than the original one-line scope suggested: room v12 (MSC4297 + MSC4289) isn't just a state-res tweak, it also changes the room ID format and the power-level model. Implemented for real rather than descoped, since a partial version-12 room would be actively misleading (claiming compliance while silently breaking on the very things that changed).

**Room version 12** (`AxonRoom.CreateRoom`, `AuthRules`, `EventBuilder`, `StateResV2`):
- **Domainless, hash-derived room IDs**: a v12 room's ID is its own `m.room.create` event's ID (reference hash) with `!` instead of `$` — no `:server_name` suffix at all. This is a real chicken-and-egg problem (the room "exists" — DB row, `RoomProcess` — only after the create event is built and hashed, but every other creation step needs the room_id first): `CreateRoom.execute/2` now builds+hashes the create event standalone (`EventBuilder.build/4` with `room_ctx.room_id: nil` omits the `room_id` field entirely, matching the wire requirement that the create event MUST NOT carry one), derives `room_id` from its hash, *then* creates the room row and injects the pre-built event via `RoomProcess.apply_remote_event/2`. `EventStore.event_to_map/1` omits `room_id` specifically for a v12 create event on every outbound path (sync, federation `/state`, `/send_join`, transactions) since it's the single canonical serialization function. Room upgrades to v12 reverse their usual order (new room created first, *then* the old room is tombstoned with the now-known new room_id) since the new ID can't be pre-generated the way every earlier version's `!random:server` id could.
- **Creator privilege (MSC4289)**: a v12 room's "creators" (the create event's `sender` plus any `additional_creators` from its content, passed like any other `creation_content` field) get implicit, infinite power — never listed in `power_levels.users` (a `power_levels` event that lists one is rejected, rule 10.4), can't be outranked or demoted. `additional_creators` is validated as an array of real user IDs at both room-creation time and on any inbound create event.
- **Auth-events selection**: `m.room.create` is excluded from every other event's `auth_events` in v12 (rule 3.2) — fixed in both the local build path (`EventBuilder`) and the federation join/knock/leave template path (`FederationController`), which duplicates that selection logic since remote joins don't go through the normal local build.
- **State-res v2.1** (`AxonRoom.StateResV2`): the one piece that actually matches the original one-line scope — iterative auth checks now start from an empty state map for v12 instead of the unconflicted state map, per MSC4297. The deeper "conflicted state subgraph"/"auth difference over the full conflicted set" refinement wasn't implemented yet at the time, and `AxonRoom.StateResolver` (this module's only caller) only ever resolved one hop of history, so the refinement wasn't reachable through the call path regardless — both fixed for real in Phase 15.6.
- **Defensive fix**: joining/knocking a domainless room_id with no `via`/`server_name` hint used to silently fall back to treating the whole room_id string as a server name (garbage). Now returns a clear `M_MISSING_PARAM` instead.

Regression coverage: `apps/axon_web/test/room_v12_test.exs` (creation, creator/additional_creators power, rule 10.4 rejection, non-creator power gating still works, federation join of a v12 room via `FakeRemoteMatrixServer`, the via-hint error).

**Third-party (3pid) invites** (`m.room.third_party_invite`, `AuthRules.valid_third_party_invite?/3`): `POST /rooms/:roomId/invite` now has a real `medium`+`address` variant that creates an `m.room.third_party_invite` state event (rule 7: gated by invite power) with a token and this server's own signing key, rather than a direct `m.room.member` invite (impossible — no mxid is known yet). `POST /join/:roomId` accepts an optional `third_party_signed` proof, which — if its `signed.token` matches a live invite event in the room and its signature verifies against that event's `public_key`(s) — authorizes the join regardless of the room's normal `join_rule`, exactly like a direct invite would.

Known, deliberate gap: axon has no identity-server integration, so nothing actually delivers a 3pid invite to the invitee (no email/SMS sent) — the room-state mechanics and cryptographic proof are fully real and spec-shaped (self-signed with this server's own key, since there's no external identity server to delegate to), but getting the token to the invitee is left to an out-of-band channel until real mail/identity integration exists. `PUT federation/v1/exchange_third_party_invite` (for a remote server to ask a room-resident server to mint the invite event on its behalf) is not implemented — it would only be reachable once an external identity server both sides trust exists, which axon doesn't provide, so it'd be dead code today.

Regression coverage: `apps/axon_web/test/third_party_invite_test.exs` (invite creation, power gating, valid-proof join, mxid-mismatch rejection, bad-signature rejection).

**Guest access**: audited against spec. The one protocol-mandated, testable guest-specific behavior — `m.room.guest_access` gating on join — was already implemented and tested (`apps/axon_web/test/e2e/guest_access_flow_test.exs`, pre-existing). Didn't find another well-established *protocol* requirement worth adding; further restriction beyond that is a server-policy choice, not a spec requirement, and guessing at additional restrictions risked incorrectly blocking guests from things they're actually allowed to do. No changes made.

## Phase 13 — Operability: admin API & rate limiting (done)

A real admin API under `/_synapse/admin/v1` (mirroring the naming convention the existing shared-secret registration bootstrap already used), gated by a new `AxonWeb.Plug.RequireAdmin` checking `users.admin` — which turned out to be a pre-existing, silently-toothless field: the `admin` flag accepted by the Synapse-compatible bootstrap registration endpoint was parsed and cryptographically bound into its MAC check, but never actually passed through to `UserStore.register/3`, so no account could ever really become an admin before now. Fixed as part of wiring the gate up (the alternative — building an admin API nothing could ever reach — wouldn't have been shippable).

- **User management**: `GET /users` (paginated, filterable by guest/deactivated status), `GET /users/:id`, `POST /deactivate/:id` (admin-initiated; refactored the existing self-service `/account/deactivate` to share the same `UserStore.deactivate/1`), `POST`/`DELETE /users/:id/shadow_ban`.
- **Shadow-ban enforcement** (not just a flag): a shadow-banned user's own non-state (message-like) events are invisible to every *other* local viewer's `/sync` — both classic and sliding — but always visible to themselves (reading your own writes must still work), and never federated out. State events (joins, etc.) stay visible everywhere regardless; hiding those would corrupt other viewers'/servers' picture of room membership, a bigger inconsistency than muting a spammer's messages. Enforced in `EventStore.get_user_events_since/2` and `get_recent_room_events/3` (local sync fan-out) and `RoomProcess`'s federation broadcast (outbound).
- **Room management**: `GET /rooms`, `GET /rooms/:id`, `DELETE /rooms/:id` — a real purge (deletes the room's events/state/memberships/account-data/receipts from local storage) that also marks the room `blocked` so a future join attempt gets a clear, specific rejection instead of silently doing nothing; also terminates the room's live process if one is resident in memory, so it can't keep serving stale state after the rows it was built from are gone.
- **Media quarantine**: `POST`/`DELETE /media/quarantine/:server/:id` — quarantined media is served as a plain 404 (matches Synapse), not a distinct error, so a client/scraper can't use the response shape to tell "never existed" apart from "admin pulled it".
- **Report-review queue**: `GET /event_reports`, paginated — reports were already being collected via the client `POST /rooms/:roomId/report[/:eventId]` endpoints but nothing had ever surfaced them until now.
- **Rate limiting**: a small self-contained ETS-backed sliding-window limiter (`AxonWeb.RateLimiter`, mirroring `AxonSync.Typing`'s GenServer+ETS pattern rather than adding a new dependency for what's a contained need), applied to login (per-IP), registration (per-IP), and message send (per-user) via a controller-level plug. Limits are runtime-configurable (`config :axon_web, :rate_limits`), which is what let `config/test.exs` set them high enough that the test suite's own traffic (many registrations/logins/sends from the same loopback IP across many files) never trips them.

Regression coverage: `apps/axon_web/test/admin_controller_test.exs` (the `RequireAdmin` gate, every admin endpoint), `apps/axon_web/test/shadow_ban_test.exs` (local sync filtering both directions, state-event visibility, federation skip via `FakeRemoteMatrixServer`), `apps/axon_web/test/rate_limit_test.exs` (the limiter directly, and the login plug's 429 shape end-to-end).

## Phase 14 — Feature completion pass (done)

- **Push rules persistence**: `PUT`/`DELETE`/`.../enabled`/`.../actions` on `/pushrules/...` used to silently discard — only the hardcoded server default rule set was ever actually served or evaluated, regardless of what a client tried to customize. Now backed by `AxonPush.UserRules`: a server-default rule (`.m.rule.*`) can have its `enabled`/`actions` overridden but not its `conditions`/`pattern` (fixed, per spec); any other rule id is a genuine custom rule, evaluated before the (possibly-overridden) defaults for its kind. `AxonPush.Dispatcher` now evaluates each recipient's actual merged ruleset instead of always the defaults. Fixed a real bug found in the process: `room`/`sender` kind rules were falling through to generic conditions-based matching, which vacuously matched *every* such rule against *every* event (an empty `conditions` array trivially satisfies `Enum.all?`) — they were simply unreachable before now since no default `room`/`sender` rules exist, but would have misfired the instant a user's custom one became reachable. Not implemented yet: the `before`/`after` positioning query params — see Phase 15.6, which closed this.
- **Server notices**: a lazily-provisioned system account (`@server-notices:<server_name>`) an admin can use (`POST /_synapse/admin/v1/send_server_notice`) to push a message into a dedicated room with any local user, auto-created and auto-joined (not just invited — an invite-only membership can't see timeline content, which would defeat the purpose of a notice) on first use and reused for every subsequent notice to that user, tagged `m.server_notice` in their room account data so a real client can render it distinctly from an ordinary DM.
- **SSRF-safe URL previews**: `GET /_matrix/client/v1/media/preview_url` (previously a deliberate 404) now really fetches and parses OpenGraph metadata, with layered SSRF defense — scheme allowlist, literal-IP and resolved-DNS rejection of private/loopback/link-local/multicast/reserved ranges (IPv4 and IPv6, including IPv4-mapped IPv6), manually-followed and re-validated redirects (capped hops), and response size/time limits. An `og:image`, if present, is fetched through the same hardened path and rehosted as local media so the response returns an `mxc://` URI rather than leaking the original image URL. Results are cached (1hr TTL). Known, documented limitation at the time: not immune to DNS rebinding (the resolve-and-validate check and the HTTP client's actual connection are two separate steps) — see Phase 15.6, which closed this by pinning the connection to the validated address.

Regression coverage: `apps/axon_web/test/push_rules_controller_test.exs` + `apps/axon_web/test/e2e/push_pipeline_flow_test.exs` (a muted room's push rule actually suppresses delivery end to end), `apps/axon_web/test/server_notices_test.exs`, `apps/axon_media/test/url_preview_test.exs` (SSRF blocking and OpenGraph parsing — tested separately by design: real network calls are asserted *rejected* rather than faked, and parsing is exercised directly against canned HTML) + `apps/axon_web/test/media_controller_test.exs`.

## Phase 15 — Production readiness & closing known gaps (done)

The roadmap's originally-planned arc (Phase 8 through 14) was complete; Phase 15 is what it took to go from "feature-complete" to "safe to actually run and to build the next big feature on top of."

### 15.1 — Security-critical config hardening + prod Docker image

`config/runtime.exs`'s `SECRET_KEY_BASE`/`DB_PASS` now hard-fail boot in prod instead of silently falling back to a hardcoded, publicly-known secret (`"a" * 64`) and `axon`/`axon` credentials. New root `Dockerfile` (prod-oriented, non-root user, `HEALTHCHECK`, expects Postgres/secrets from the environment, never auto-runs migrations on boot — distinct from `complement/Dockerfile`, which stays as the Complement test-harness image), `.dockerignore`, `docker-compose.yml`, `.env.example`.

### 15.2 — CI/CD pipeline

`.github/workflows/ci.yml`: test suite against a real Postgres service container, format check, unused-deps check, non-blocking dialyzer (no prior warning baseline, so it reports rather than gates), and a Docker build smoke test of the Phase 15.1 image.

### 15.3 — Observability: telemetry, health endpoints, Sentry

`AxonWeb.Telemetry` wires the previously-declared-but-dead `telemetry`/`telemetry_poller` deps to real Phoenix/Ecto/VM/mailbox-depth metrics, surfaced via Phoenix LiveDashboard at `/_synapse/admin/dashboard` (gated by the existing admin auth pipeline). `GET /health` (liveness) / `GET /ready` (readiness, checks the DB) back the Phase 15.1 Dockerfile's `HEALTHCHECK`. `user_id`/`room_id` now actually populate the Logger console metadata `config/config.exs` had long declared but nothing ever set. Sentry added — no-ops unless `SENTRY_DSN` is set, logger handler only attaches when it is. (Its transport dependency was cleaned up in 15.6 below.)

### 15.4 — Resilience: rate limits + OutboundQueue circuit breaker

Rate limiting (Phase 13's `AxonWeb.RateLimiter`) extended to media upload, URL preview, search, and sync (sync gets a much higher limit than the others since legitimate clients long-poll it continuously). `AxonFederation.OutboundQueue` now caps concurrent in-flight delivery attempts per destination and trips a circuit breaker after 5 consecutive failures — exponential backoff alone only spaced out individual rows' own retries, not the concurrency across different rows becoming due against the same down remote server at once.

### 15.5 — Real-client E2E harness (Playwright + Element Web)

New `e2e-client/` directory: real Element Web instances (matrix-js-sdk) driven by two Playwright browser contexts (Alice + Bob) messaging each other through the actual UI — distinct from `apps/axon_web/test/e2e/*.exs` and `complement/`, both of which are black-box but drive raw HTTP against the Matrix API rather than a real client. Local-dev profile (Axon via `mix phx.server`) and CI profile (the Phase 15.1 prod Docker image) via docker-compose; `.github/workflows/e2e-client.yml` runs it nightly + on manual dispatch (too slow for the PR-blocking loop). First scenario: a DM message round-trip through Element's default encrypted-DM flow. A follow-up fix corrected `element-config.ci.json` pointing the (host-run) browser at a Docker-internal hostname it can't resolve, and installed ImageMagick on the CI runner for `ThumbnailerTest`.

Later in this phase, a second real scenario (`verification-request.spec.ts`) was added while investigating a user report that cross-signing device verification "does nothing" between two users: two real Element Web clients, Alice and Bob, actually driving matrix-js-sdk's own cross-signing bootstrap and verification-request UI against a live Axon server. It passes — proving the server-side mechanism (to-device delivery, cross-signing key upload/query, EDU federation) genuinely works. The real cause of the report was client-side: Element's "Set up Secure Backup" cross-signing bootstrap prompt is a dismissible toast, not a blocking step, and skipping it leaves a user unable to present a clean verification flow to others — a UX trap, not a protocol requirement (you *do* need your own cross-signing keys before you can meaningfully verify someone else, but Element doesn't make that obvious). Also fixed a real, previously-undiscovered bug in `element-config.local.json` (`base_url` pointed at `host.docker.internal`, unreachable from a host-run browser).

### 15.6 — Real state resolution + closing remaining known gaps

The biggest remaining correctness gap: `AxonRoom.StateResolver.resolve_for_auth_check/3` only ever looked one hop into a `prev_event`'s own `auth_events`, so no conflict it produced could span more than one generation — making MSC4297's room-v12 "conflicted state subgraph" refinement (noted as a gap since Phase 12) provably unreachable dead code. Separately, `RoomProcess` applied a forked PDU by overlaying it onto the *old* `current_state` rather than the resolved one, so even that shallow one-hop resolution only ever gated the auth check and was then discarded — a room's state after a real fork was last-write-wins by local insertion order, not spec state resolution.

Fixed in four dependent parts (`apps/axon_room/lib/axon_room/state_res_v2.ex`, `state_resolver.ex`, `room_process.ex`, new `AxonCore.EventStore.get_event_maps/2`):
- **Corrected conflicted/unconflicted split**: per spec, a key present in only *some* input state sets is conflicted (must survive auth-checking to appear in the result at all), not a free pass — the prior split treated it as unconflicted.
- **Real recursive state-at-event resolution**: replaced the one-hop branch builder with a capped, batched, cycle-safe two-phase BFS walk (prefetch, then a memoized fold) that resolves state as far back through locally-known history as a PDU's ancestry actually requires — this is what makes multi-generation forks, and therefore the v12 refinement, reachable at all.
- **`RoomProcess` rebases onto resolved state**: after a fork, `current_state` (and therefore `/sync`, `/state`, restricted-join checks, directory/profile lookups — everywhere room state is read) reflects the real resolved winner going forward, not whatever was last blindly overlaid.
- **The v12 conflicted state subgraph itself**: implemented as a single memoized bottom-up pass (not per-node, avoiding an O(V²) regression) over the conflicted event chain, gated strictly on room v12.

Two more previously-latent bugs surfaced by testing deep enough forks to reach these new paths, both fixed: a sign error in the depth tie-break (`reverse_topological_power_ordering`) that let an older ancestor power-levels event outrank its newer descendants, and a missing spec-mandated `auth_events` fallback in the iterative auth-check loop.

**Federation backfill/catch-up** (new `AxonFederation.Backfill`, `AxonFederation.EventVerification`): closes a related gap the state-res work explicitly flagged as out of scope — when an inbound PDU's `prev_events` reference events axon has never seen, nothing ever fetched that missing history from the origin. Now `AxonWeb.FederationController.process_inbound_pdu/2` detects the gap and calls the origin server's `POST get_missing_events` first (the common "missed one live transaction" case), falling back to `GET backfill` if that doesn't fully close it; fetched events are verified and applied one at a time via the normal `RoomProcess.apply_remote_event/2` path (signature check + auth check + state resolution), oldest-first, same as a live `/send` PDU — they are not pre-vetted the way a `send_join` state snapshot is. Also fixed, while in this code path: `process_inbound_pdu/2`'s unknown-room check used `unless ... do ... end` and discarded the result, so an unknown-room PDU fell through to full signature verification and (now) a wasted catch-up round-trip instead of being rejected immediately.

**Other closed gaps**:
- Push rules now honor `before`/`after` positioning (`PUT pushrules/.../{kind}/{ruleId}`) instead of always appending in creation order (closes the Phase 14 gap).
- URL preview fetches (`AxonMedia.UrlPreview`) now pin the HTTP connection, via Mint, to the exact address that was SSRF-validated, closing the resolve-then-connect DNS-rebinding gap the Phase 14 SSRF hardening left open.
- Sliding sync computes real per-room `notification_count`/`highlight_count` from the user's actual push rules (capped scan) instead of always returning 0, and surfaces invited/knocked rooms (`invite_state`/`knock_state`), reusing classic sync's invite-state visibility rules (promoted `build_invite_state/2` into shared `AxonWeb.SyncHelpers` so the two sync implementations can't drift) — both close Phase 10 gaps.
- Dependency vulnerabilities: `bandit`/`mint`/`phoenix`/`postgrex`/`plug`/`phoenix_live_view` bumped to their latest compatible releases; `sentry` bumped 10.10.0 → 13.3.0 specifically to reach the version where its default HTTP client switched from Hackney to Finch (12.0.0), letting the explicit `hackney` dependency — carrying its own known CVEs and otherwise unused directly in this codebase — be removed entirely.

Regression coverage: `apps/axon_room/test/state_res_v2_test.exs` + `state_resolver_test.exs` + `apps/axon_web/test/state_resolver_test.exs` (corrected split, multi-generation forks producing genuinely different resolved outcomes, cycle termination, v11-vs-v12 subgraph comparison), `apps/axon_web/test/federation_controller_test.exs` (get_missing_events catch-up, backfill fallback), `apps/axon_push/test/user_rules_test.exs` + `apps/axon_web/test/push_rules_controller_test.exs` (rule positioning), `apps/axon_web/test/sliding_sync_test.exs` (notification/highlight counts, invite/knock visibility), full `mix test` (849 tests) green after every dependency bump.

## Phase 16 — A real Complement number, a real bug it found, and closing two more Phase 15 gaps

The Docker Engine/Complement client-library version skew noted at the end of Phase 15 (`getHostAccessibleHomeserverURLs` breaking on `TestVersionStructure` itself) did not reproduce in this environment's Docker Engine — `TestVersionStructure` passed cleanly on its own, and a full `./tests/...` run completed end to end for the first time. Getting there also needed two more real harness-vs-server mismatches fixed, both committed ahead of the run:

- **Federation TLS on port 8448**: Complement connects to the federation port directly and expects TLS (normally a reverse proxy terminates this in front of axon). `complement/start.sh` now signs a cert off Complement's own shared CA and exports `FEDERATION_TLS_{CERT,KEY,CA}_FILE`; `config/runtime.exs` configures `AxonWeb.FederationEndpoint` to serve HTTPS on 8448 when present, and `:federation_extra_cacertfile` lets outbound federation (`Axon.Finch`) trust that same CA so two Complement-spawned homeservers validate each other normally.
- **Rate limiting**: Complement's own registration/send traffic is far faster than the production anti-abuse limits allow. `RELAXED_RATE_LIMITS=true` (set by `start.sh`) pushes every limit's `max` to 1,000,000 in `runtime.exs`, mirroring `config/test.exs`'s existing approach — `rate_limit_test.exs` still covers the limiter's real behavior regardless.

**A real, previously-unknown bug, found by Complement and nothing else**: the first full run's `tests` (federation-heavy) package failed almost everything — including plenty of scenarios covered elsewhere by axon's own passing test suite, which was the tell that something systemic, not many independent gaps, was going on. `RoomController.resolve_room/3`'s alias-join branch built its federation `via` list from `params["server_name"]` and handed it straight to `Enum.find_value` — but Phoenix/Plug hands a single `?server_name=x` back as a bare string, not a list, so any alias join or knock carrying exactly one via-hint (the common case, and one Complement's own test setup uses constantly) crashed with a 500 instead of the clean 404 a not-found room should give. The room_id (`!`) branch already guarded against this with `List.wrap`; the alias (`#`) branch didn't. Fixed by wrapping unconditionally inside `resolve_room/3`, plus a related, previously-latent spec gap found in the same code: Plug's query parser silently collapses a *repeated* `?server_name=a&server_name=b` down to just the last value, so axon could never have honored more than one via-hint even before this bug — `server_name_hints/1` now reads every occurrence directly off the raw query string. 3 new regression tests in `room_controller_test.exs`.

**The numbers, honestly, with caveats**: this environment is memory-constrained (14GB, shared with several other unrelated projects' resident Docker containers), and Complement's full container fleet is heavy enough that a first attempt run alongside this repo's own `mix test` suite caused enough memory pressure to silently kill the whole run with no output — not a Complement or axon problem, a "don't run two heavy test suites at once on this box" problem. Isolated re-runs were stable. The dozen `msc*` packages (`msc2836`, `msc3391`, `msc3757`, `msc3874`, `msc3890`, `msc3902`, `msc3930`, `msc3967`, `msc4140`, `msc4155`, `msc4222`, `msc4306`) fail effectively 100% of the time — expected, not a regression: these are all still-unstable MSCs axon has never claimed to implement (thread subscriptions, various sync/auth experiments), not gaps in anything on this roadmap. The two packages that actually test what axon claims to support:
  - **`tests/csapi`**: 46/64 top-level tests passed (~72%) before hitting a real, reproducible problem (below) that forced the package's own timeout both times it was run.
  - **`tests`** (general + federation): only 10/89 passed (~11%) — after the `server_name` crash fix, not before it (that fix alone didn't move this package's rate much; whatever's failing here is mostly a different, larger set of individually-untriaged gaps — ACLs, device-list-over-federation edge cases, restricted-room joins, media, and more — each would need its own investigation this phase didn't have time for).
  - **A reproducible near-hang, not yet root-caused**: `TestMessagesOverFederation`'s `messagesRequestLimit`-below-backfill-count subtest blocked for 28+ minutes on a single `GET /rooms/:id/messages` call from a just-joined-via-federation user with no response at all (confirmed via the Go client's own goroutine dump — it was still waiting on the HTTP round-trip, not retrying or polling), forcing the whole `csapi` package to its timeout in both the first and the follow-up run. Read through the handler chain (`EventController.get_messages/2` → `member_or_forgotten?/2` → `EventStore.get_messages/4`): all three are plain, non-blocking, already-indexed Ecto queries with no `GenServer.call`, no federation HTTP call, and no loop — nothing in the code as read explains a genuine multi-minute hang. The host was independently confirmed to be under severe memory/swap pressure during both occurrences (free memory in the low hundreds of MB, several GB swapped). Left genuinely unresolved rather than guessed at: this could be swap-thrashing-induced pathological query latency on a constrained host, or a real axon-side stall this phase didn't have the clean environment to isolate. **Needs a re-run on a less memory-constrained machine to get a definitive answer** — noting explicitly rather than either claiming a fixed bug that wasn't fixed or burning more of this environment's limited memory chasing it further.

**Sliding sync bandwidth diffing** (closes the Phase 10 gap below): `AxonWeb.SlidingSync.ConnState` (ETS, TTL-swept) remembers, per client-chosen `conn_id`, the room_ids last sent for each list range and the full entry last sent for each room. A request with no `conn_id` is completely unaffected (always full `SYNC`, always full room entries — verified unchanged by a dedicated test); one that reuses the same `conn_id` gets a range's op omitted when unchanged and a room omitted from `rooms` outright when its content is byte-for-byte identical to last time, while a reorder or content change still forces a resend. 5 new tests in `sliding_sync_test.exs`.

**Signing key persistence**: closed a standing TODO in `AxonCrypto.KeyServer` — it always minted a fresh Ed25519 keypair in memory with nowhere to reload it from, so every restart silently changed the server's federation identity, invalidating every cached `/_matrix/key/v2/server` response and signature another server had already verified. Now persists to an optional `SIGNING_KEY_PATH` (`config/runtime.exs`, prod only); unset keeps the old always-fresh behavior exactly, which is what dev/test/CI still get. `docker-compose.yml`/`Dockerfile`/README updated with a default persistent-volume path. 4 new tests.

Regression coverage this phase: `apps/axon_web/test/room_controller_test.exs` (3, the `server_name` crash), `apps/axon_web/test/sliding_sync_test.exs` (5, conn_id diffing), `apps/axon_crypto/test/axon_crypto/key_server_test.exs` (4, signing key persistence) — full `mix test` green throughout (883 tests) in isolation from the memory-heavy Complement runs.

## Phase 17 — Triaging the Complement `tests` package

Phase 16 left the `tests` package (general + federation black-box conformance) at **10/89 (~11%)** with the failures explicitly untriaged: "whatever's failing here is mostly a different, larger set of individually-untriaged gaps — ACLs, device-list-over-federation edge cases, restricted-room joins, media, and more". This phase worked through that list cluster by cluster. It ends at **22/89 (~25%)** with **zero regressions** — still a minority, and the package still exits FAIL. The more useful result than the number is *what* the failures turned out to be: a few were one systemic bug each holding down a whole group of tests, and several were entire endpoints or features that had simply never been implemented at all (federation invites in both directions, `event_auth`, `/context`, the authenticated federation media endpoints, and server-ACL enforcement anywhere).

### The systemic one: `send_join`/`send_leave`/`send_knock` never relayed onward

`RoomProcess.apply_remote_event/2` never re-broadcast an applied PDU to federation at all. That's correct for an ordinary `/send` transaction PDU — the origin server already pushed it to every resident server itself, and relaying further would duplicate traffic and risk amplification loops. But it's wrong for `send_join`/`send_leave`/`send_knock` specifically: the acting user's own server structurally *cannot* fan the new membership event out to the room's other servers, because it only just learned the room exists (and who is in it) from this very request, and has only ever talked to the one resident server it went through. The resident server is the only party with the full membership picture — and it was silently telling nobody.

The consequence is that **a room with three or more homeservers never converged on membership**. Each newly joined/knocked/left server's peers never learned about it unless they happened to receive the event by some other route. This is what made `TestACLs` fail on its own control group (the sentinel room, which has no ACL at all): charlie could not see bob's message because charlie's *join* had never been relayed to bob's server in the first place.

Fixed with an opt-in relay: `RoomProcess.apply_remote_event/3` takes `relay_exclude: server_name`, which — only when set — fans the just-applied event out over the same `federation:fanout` PubSub path local sends use, excluding the server that just handed us the event (it already has it). `FederationController` passes the requesting origin from all three `send_*` handlers; every other call site (ordinary `/send` PDUs, `Backfill` catch-up, v12 room creation) keeps the previous no-relay default unchanged.

Found in the same code while adding coverage: `send_leave` had **no type/content validation whatsoever**, unlike its `send_join`/`send_knock` siblings — any signed, auth-valid event from a joined member (an ordinary message, say) was accepted and applied through it. And all three now reject a mismatched `state_key`/`sender` with `400 M_BAD_JSON` directly rather than falling through to `AuthRules` and surfacing as `403`; these are self-action-only endpoints, so a mismatch is a malformed request, not an authorization failure.

### Server ACLs were enforced nowhere

`m.room.server_acl` appeared in exactly two places in the codebase — as a default power-level entry in `CreateRoom`, and in `RoomUpgrade`'s copied-state list. Nothing ever *evaluated* one. A server named in a room's `deny` list kept sending messages, joining, and posting read receipts entirely unimpeded.

New `AxonRoom.ServerAcl` implements the spec's algorithm in order (no ACL event → allow; IP literal with `allow_ip_literals: false` → deny; `deny` match → deny; `allow` match → allow; otherwise deny), with case-insensitive glob matching (`*`, `?`), the port stripped before matching (`evil.com`, `evil.com:8448` and `evil.com:1234` must all match a rule for `evil.com`), and bracketed-IPv6 handling so `[::1]:8448` isn't shattered by a naive split on `:`. Deliberately kept out of `AuthRules`: the spec is explicit that ACLs "do not restrict the events relative to the room DAG via authorisation rules, but instead act purely at the network layer", so a server denied by an ACL added *after* it already has events in the room keeps them — only its further traffic is rejected.

Wired into every endpoint the Server-Server API's ACL section marks MUST-protect (`make_join`/`make_leave`/`make_knock`, `send_join`/`send_leave`/`send_knock`, `state`, `state_ids`, `backfill`, `get_missing_events`, `event_auth` → `403 M_FORBIDDEN`), plus per-PDU gating on `/send` (soft-failed, not applied) and per-EDU gating on `m.typing`/`m.receipt` (dropped).

Chasing the EDU half of this surfaced an unrelated real bug: an **initial** (`since`-less) `/sync` attached an `m.typing` ephemeral entry to *every* joined room even when nobody was typing. `build_typing_event/1` assumed — correctly for incremental sync, per its own comment — that a room only appears in the response because something ephemeral changed, making an empty `user_ids` list a meaningful "someone stopped typing" transition. That assumption doesn't hold for an initial sync, which includes every joined room unconditionally. The ACL-denied room in `TestACLsForEDUs` was leaking exactly this spurious entry.

### Three endpoints that were never implemented

- **Federation invites** (`PUT /_matrix/federation/v2/invite/...`, both directions). Inviting a remote user was a complete no-op from their side: the CS API invite handler always built a *local* `m.room.member` event regardless of the target's server, and nothing anywhere spoke the federation invite handshake — no outbound call, and no inbound route either, so a remote server inviting one of our users just got a 404. Outbound now builds and self-signs the event through the same `EventBuilder`/`AuthRules` a local invite uses (so an inviter without invite power is rejected up front, not only in the same-server case), round-trips it through the target's endpoint to collect their signature, and applies the result via the normal remote-event pipeline. Inbound validates the event and persists it *without becoming resident* — a bare membership row plus the sender's `invite_room_state` preview, which is exactly what `invite_state` needs, mirroring how knocks already store a preview. Rejecting such an invite needed its own path too (`AxonFederation.RoomLeave`): a room known only via a federated invite has no local `RoomProcess` to leave through, so `POST /rooms/:id/leave` now detects the absence of a local create event and goes out via `make_leave`/`send_leave` instead of silently discarding the invite locally.
- **`GET /_matrix/federation/v1/event_auth`** — no route at all. Returns the requested event's full transitive auth chain (ancestors only), reusing the same recursive walk `get_state`/`get_state_ids` already use.
- **`GET /rooms/:room_id/context/:event_id`** — no route at all, so it 404'd generically. Returns the target event plus a window of surrounding timeline (split from `limit`, reverse-chronological before / chronological after, target excluded from both) and the room's current state, behind the usual membership gate.

### Media: authenticated federation media, and a filename that was thrown away

Remote media fetching spoke *only* the deprecated, unauthenticated `/_matrix/media/v3/...` endpoint, hardcoded to `https://<origin_server>/` — no federation TLS port resolution, no X-Matrix signing. That only ever worked against a server reachable on plain port 443 with no auth required, which is never true of another axon/Complement instance and increasingly untrue of any spec-compliant server. There was no inbound support at all either: nothing served axon's own media to another server over the authenticated endpoint.

Both directions now exist (MSC3916 / Matrix 1.11): `GET /_matrix/federation/v1/media/{download,thumbnail}` serves local media as `multipart/mixed`, and `AxonFederation.MediaFetch` fetches from the same endpoint on the remote, parsing the multipart response (including a `Location` redirect in the file part). Fallback to the deprecated endpoint (with `allow_remote=false`, per spec) happens on a `404 M_UNRECOGNIZED` — the spec's defined signal — and also on a `400`, which is not spec-mandated but is what a server whose authenticated-media handler is broken or absent tends to answer a well-formed GET with; hard-failing a user's download over that helps nobody when the deprecated endpoint works. A `403` or `5xx` still propagates as-is. (Complement's own federation test double is such a server: `HandleMediaRequests` reuses the legacy handler — which validates an `{origin}` path variable — for the authenticated route, whose path has no such variable, so it unconditionally 400s. Preferring the authenticated endpoint is what briefly regressed `TestMediaWithoutFileName` mid-phase.)

Separately: `?filename=` was parsed at upload time and **immediately discarded**, and every download hardcoded `Content-Disposition: inline` with no filename at all. Now persisted (new `media.filename` column) and returned, with `inline` reserved for the spec's "safe to render" content-type allowlist and `attachment` for everything else; ASCII names go out as a quoted-string (which handles embedded spaces and semicolons once quoted), non-ASCII via RFC 6266's `filename*=UTF-8''<pct-encoded>` form. Proxied remote downloads carry the remote's filename through too.

Fixing outbound media also exposed a latent bug in `AxonFederation.ServerResolver`: a failed `.well-known` lookup appended a *second*, default `:8448` even when the `server_name` already carried its own port, producing an unparseable double-port authority. An explicit-port `server_name` now skips well-known entirely, per the server-name resolution spec. Unreachable before, since nothing had resolved an arbitrary explicit-port `server_name` through that path.

### Room v12: the create event's `room_id` was being stripped from *client* responses too

MSC4291's "the create event carries no `room_id`" rule is a **federation PDU** rule — the room ID is that event's own reference hash with `!` for `$`, so the field is redundant on the wire, and its presence would change the content hash and make a receiving server compute a different event ID. Phase 12 implemented it inside `EventStore.event_to_map/1`, described at the time as "the single canonical serialization function" used on every outbound path. That description was precisely the problem: the same function also feeds the entire Client-Server API, whose `ClientEvent` schema requires `room_id` unconditionally. Every client-facing view of a v12 create event — `/sync`, `/state`, `/messages`, `/event/:id`, `/context`, `/state?format=event` — was silently missing it.

Split into two explicit functions: `event_to_map/1` (client form, always keeps `room_id`) and `event_to_pdu/1` (federation form, omits it for a v12 create only). All eight serialization sites in `FederationController` and both `broadcast_for_federation` call sites in `RoomProcess` use the PDU form; every CS API caller keeps the client form. Two existing tests asserted the old behavior through the client form and were corrected to pin the real distinction — both now assert *both* forms explicitly so the two can't silently re-converge.

### Regression coverage this phase

Every fix above landed with tests in the normal `mix test` suite, not only a Complement pass:

- `apps/axon_room/test/server_acl_test.exs` (12, new file) — the ACL algorithm itself: deny-over-allow precedence, glob wildcards, case-insensitivity, allow-list default-deny, port stripping, `allow_ip_literals` incl. bracketed IPv6, `?` single-char globs.
- `apps/axon_web/test/federation_controller_test.exs` (+12) — 7 for ACL enforcement end to end (PDU soft-fail, pre-existing membership preserved, `make_join`/`send_join`/`state`/`state_ids`/`backfill`/`get_missing_events` 403s, typing and receipt EDUs dropped); 3 for the `send_join`/`send_leave`/`send_knock` relay against two independent fake remote servers (with unique per-test ports/server-names, so `OutboundQueue`'s global per-destination circuit-breaker state can't bleed between tests); 2 for `event_auth`.
- `apps/axon_web/test/e2e/federation_room_lifecycle_test.exs` (+2) — the full inbound-invite → `/sync` → federated-rejection flow, and the full outbound-invite → federation request → applied-locally flow.
- `apps/axon_federation/test/media_fetch_test.exs` (9, new file) — multipart parsing, ASCII and RFC 6266 filename extraction, `Location` redirect following, fallback on `404 M_UNRECOGNIZED` and on `400`, and *no* fallback on `403`.
- `apps/axon_web/test/media_controller_test.exs` (+9) — the federation media endpoints (inbound multipart shape, 404, outbound authenticated fetch) and six `Content-Disposition` cases (round-trip, semicolons, Unicode, URL override, omission, inline-vs-attachment).
- `apps/axon_web/test/room_v12_test.exs` (+4) — all five client-facing views carry `room_id`, the PDU form still omits it, plus non-create-v12 and v11-create control cases.
- `apps/axon_web/test/event_controller_test.exs` (+3) — `/context` window shape, 404 for unknown/wrong-room event, 403 for non-member.
- `apps/axon_media/test/store_test.exs` (+1), `apps/axon_federation/test/server_resolver_test.exs` (+2), `apps/axon_web/test/phase9_ephemeral_test.exs` (+1), `apps/axon_core/test/event_store_test.exs` and `apps/axon_room/test/create_room_test.exs` (2 stale tests corrected — see below).

Two pre-existing tests asserted the *old, wrong* v12 behavior through the client serialization form and were rewritten to pin the real PDU-vs-client distinction instead; both now assert both forms explicitly so the two can't silently re-converge. Full `mix test` green at the end of the phase: **938 tests, 0 failures** (up from 883), always run in isolation from Complement per the memory constraints noted in Phase 16.

### Where it stands, honestly

**22/89 (~25%)**, up from 10/89 (~11%) — verified by a full-package run against a freshly built image at the end of the phase, with **no regressions** (every test passing in the preceding run still passed). That is real movement, but it is still a minority: the package as a whole continues to exit FAIL, and 67 tests remain red.

The remaining 67 have been *grouped*, not individually root-caused, and it would be wrong to imply otherwise. The clusters, as the next phase's triage surface:

- **`send_*` validation (7)** — `TestCannotSendNonJoinViaSendJoin{V1,V2}`, `TestCannotSendNonLeaveViaSendLeave{V1,V2}`, `TestCannotSendNonKnockViaSendKnock`, `TestCannotSendKnockViaSendKnockInMSC3787Room`, `TestBannedUserCannotSendJoin`. This phase tightened these endpoints' validation (state_key/sender mismatch now 400, `send_leave` gained validation at all) without closing the tests, so the remaining assertions go beyond what was fixed — probably the most tractable next target, since the surface is small and well understood.
- **Restricted rooms and knocking (~15)** — `TestRestrictedRooms*`, `TestKnocking*`, `TestKnockRoomsInPublicRoomsDirectory*`. Two restricted-room *fail-over* variants went green with the relay fix, which hints that some of the rest is also multi-server convergence rather than restricted-join logic proper; that is a hypothesis, not a finding. No knocking test moved this phase.
- **MSC4289 privileged room creators (6)** — one was spot-checked: `TestMSC4289PrivilegedRoomCreators` fails on a room creator's kick being refused with `M_FORBIDDEN "Insufficient power level"`, pointing at creator-privilege handling in the kick path specifically rather than anything touched this phase.
- **Spaces / hierarchy / summary (4)** — `TestClientSpacesSummary`, `TestFederatedClientSpaces`, `TestRestrictedRoomsSpacesSummary*`, `TestRoomSummaryAllowedRoomIDs`. (`TestClientSpacesSummaryJoinRules` went green.)
- **Auth-chain and rejection handling (4)** — `TestCorruptedAuthChain`, `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, `TestUnrejectRejectedEvents`, `TestEventAuth`. The last is notable: the endpoint now exists and returns a correct chain, but the test still fails, so its assertions go beyond mere presence.
- **Device lists / to-device over federation (3)**, **MSC4297 state-res v2.1 (2)**, **MSC4311 stripped state (1)**, and assorted others (`TestJumpToDateEndpoint`, `TestUnknownEndpoints`, `TestIsDirectFlag*`, `TestSendJoinPartialStateResponse`, `TestNetworkPartitionOrdering`, `TestContentMediaV1`, …).

### Known gaps still open after Phase 17

- **Identity-server-mediated 3pid invites** (Phase 12): unchanged and deliberately untouched this phase — still no actual email/SMS delivery of a third-party invite token. The self-signed, spec-shaped room-state mechanics exist, but getting the token to the invitee needs real identity-server integration axon doesn't have, and needs an owner decision on which/whether to stand one up before it's actionable at all.
- **Complement `tests` package: 67/89 still failing (~75%)**: down from 79, and now grouped into the clusters listed above rather than being wholly undifferentiated — but only a handful have been individually root-caused, so the bulk of the triage the Phase 16 note called for is still ahead. The `TestCannotSendNon*` validation family (small, well-understood surface that this phase already partly moved) and the MSC4289 creator-privilege cluster (one member spot-checked to a specific symptom in the kick path) look like the most tractable next targets. The restricted-rooms cluster *may* partly resolve as further multi-server convergence issues are found — two of its fail-over variants went green with this phase's relay fix — but that is an inference, not a diagnosis, and no knocking test moved at all.
- **The `TestMessagesOverFederation` near-hang** (Phase 16): **did not reproduce.** Phase 16 saw it block 28+ minutes and force the package to its timeout on both runs it attempted; this phase ran the full package four times, and it did not recur once — the final run completed end to end in ~1359s with no timeout and no hang. That is meaningful evidence for Phase 16's own swap-thrashing hypothesis (the host was under severe memory pressure then, and materially less so during most of these runs) and against a real axon-side stall. It is *not* proof, and nothing was changed that would explain fixing it: no code touched this phase goes anywhere near `EventController.get_messages/2`. Recording it as "most likely host memory pressure, not an axon bug" rather than as fixed — if it never recurs on a quieter machine, it can be closed outright.
- **The `msc*` packages** remain expected failures, unchanged from Phase 16: still-unstable MSCs axon has never claimed to implement.

## Phase 18 — Parallel-agent compliance push: 22/89 → 35/89

Worked as a fleet: an Opus coordinator holding `master`, a spec auditor, two read-only triage analysts, and nine implementation workers each in its own git worktree with an isolated test database (`AXON_TEST_DB`, added to `config/test.exs` for exactly this). Every worker's brief forbade running Complement — the coordinator owned full runs — and required regression coverage in the normal `mix test` suite rather than a Complement pass alone.

**The headline number is measured, not predicted: 35/89 (~39%), up from 22/89 (~25%).** Full `mix test` ends the phase at **1092 tests, 0 failures** (up from 938).

### The bug that mattered most, and why no amount of reading found it

`make_join` answered `403 M_FORBIDDEN "User ID domain does not match origin"` for any user whose server name carries an explicit port. 26 call sites across 8 files derived a Matrix ID's server with

```elixir
id |> String.split(":") |> List.last()
```

which returns the **port**, not the host: `"@charlie:host.docker.internal:41491"` yielded `"41491"`. Every federation endpoint comparing that against the requesting origin rejected the request. Complement addresses every remote homeserver as `host.docker.internal:<port>`, so this failed the *setup* of whole test families long before their actual assertions ran.

This is why an earlier worker's careful static analysis of the `send_join`/`send_leave`/`send_knock` validation cluster correctly found **no bug** — it traced every assertion in Complement's shared helper to code that already satisfied it, and said so, and reported that if those tests were still red the cause had to lie outside that file. It did. The lesson is recorded here deliberately: a well-argued negative result from a worker is a finding, not a failure, and overruling it would have meant "fixing" correct code.

Replaced by `AxonCore.MatrixId.server_name/1` — split on the *first* colon, since sigil and localpart can never contain one — returning `nil` for an ID with no server part at all, notably a room-v12 room ID (a bare hash by design, MSC4291). `room_controller`'s v12 via-hint check had been detecting that case by relying on the old buggy behaviour (`room_server == room_id`) and now tests the `nil` directly.

### Real bugs found and fixed

- **A knocking user could join without an invite.** `check_join`'s `knock` branch accepted `knock` as a valid prior membership for a self-service transition to `join`. Spec rule 4.3.4 permits only `invite`/`join`. Found by a worker while fixing something adjacent, and not by the triage that preceded it.
- **Room creators could be outranked.** `@infinite_power` was `1_000_000_000`, but a power level is a JSON number a client may legitimately set as high as `2^53 - 1`. An admin promoted above the constant outranked the room's own creator, and the creator failed to outrank them — both directions broken. Now `10^18`, with a new auth rule rejecting any `power_levels` value outside the canonical-JSON-safe integer range.
- **Rejected events were never persisted.** The `rejected`/`soft_failed` columns were read in a dozen places and written in *none*; an event failing auth was silently dropped. Now stored with `rejected: true`, excluded from derived state, and "unrejected" with a fresh `stream_ordering` on a passing resend (a stale ordering would sort the event before any `since` token a client already holds, so it would never appear in `/sync`).
- **Events with unknown ancestry were accepted anyway.** When a PDU's `prev_events` could not be resolved, `StateResolver` fell back to the room's *current* state, authorising the event against unrelated state. It now refuses. Narrowed deliberately to "all prev_events unresolvable" so the existing, tested partial-merge behaviour survives.
- **`m.device_list_update` existed nowhere.** Phase 8 built the local device-list machinery; nothing ever federated it, in either direction. A remote server was never told a local user's devices changed.
- **`m.room.server_acl`, `/publicRooms` and aliases**: `/publicRooms` never returned `join_rule` or `room_type` at all, breaking the public-room half of its own test as well as the knock half; `DirectoryController.can_manage_aliases?/2` did its own power arithmetic and so did not know about v12 creator privilege.
- **Space hierarchy** recursed into any room's `m.space.child` links rather than only spaces, was breadth-first where the spec's pagination requires depth-first pre-order, had no pagination at all, and never consulted a restricted room's `allow` list when deciding visibility.

### Endpoints that simply did not exist

`GET /_matrix/client/v3/notifications`; room tags (`/user/{userId}/rooms/{roomId}/tags`); generic profile fields (stable since Matrix 1.16 — verified on the spec page before building, rather than trusted from the audit); `timestamp_to_event` in both the client and federation directions; `room_summary`; federation `hierarchy`; federation `version`; `power_level_content_override` on `createRoom`; and proper `404`/`405 M_UNRECOGNIZED` handling for unknown endpoints, which previously answered `M_NOT_FOUND` and had no notion of 405 at all.

### Unread notification counts

Phase 10 recorded these as always-0. That was already half-stale — Phase 15 had implemented real counts for sliding sync — so the work was to promote that logic into `AxonWeb.SyncHelpers` shared by both sync endpoints (so they cannot drift), and add a persisted notifications ledger written by `AxonPush.Dispatcher` to back `GET /notifications`. Counts stay on-the-fly rather than in a counter table: a counter must remain correct as a read receipt moves *backwards* as well as forwards, and silent drift is worse than a bounded rescan. `unread_thread_notifications` is deliberately absent — there is no per-thread receipt model to compute it from.

### Infrastructure traps found the hard way

- **The host's Postgres was dead** for the whole phase: the box now has PostgreSQL 18 binaries against a v16 data directory, so the service refuses to start. Not migrated — that data directory is shared with unrelated projects and the migration is the owner's call. Worked around with a disposable `postgres:16-alpine` container.
- **The test suite cannot be run twice concurrently on one host.** `AxonFederation.FakeRemoteMatrixServer` binds fixed ports; a collision crashes the listener and cascades into the whole `axon_web` application shutting down, which presents as 440 of 441 tests failing with a missing ETS table. Environmental, not a regression — but indistinguishable from catastrophe if you only read the summary.
- **The README's "fast incremental" Complement image rebuild cannot work on this host, and fails silently.** It copies a release built on Fedora (glibc 2.38) into a Debian bookworm image (glibc 2.36); every homeserver container then dies at boot with a `GLIBC_2.38 not found` link error, and Complement *hangs* waiting on servers that never come up rather than failing loudly. Cost a wasted 23-minute run. `_build/` is excluded in `.dockerignore` precisely to prevent this; the exclusion was removed during this phase to "fix" the recipe and had to be put back, with the reason recorded in both files. A single-test smoke check before any long run is now the standing practice.
- **A 500ms poller with no Ecto sandbox ownership** (the new device-list fan-out) crashed on every tick under test, exhausted its supervisor's restart intensity, and took down the `axon_federation` application — failing 96 unrelated tests with a missing-ETS-table error that pointed nowhere near the cause. Failed polls are now skipped.

### Where it stands, honestly

**35/89.** The package still exits FAIL and 54 tests remain red. What changed beyond the number is the quality of the remaining information: there is now a per-test first-failure extract for every failing test, so the next pass starts from evidence rather than from test names.

Known and unresolved at the end of this phase:

- **A second systemic federation bug is still open.** Outbound `make_join` to a peer is answered `401` and the *receiving* server logs nothing — which should be impossible, since every rejection path in `AxonWeb.Plug.FederationAuth` logs a warning. This gates `TestJoinViaRoomIDAndServerName`, `TestOutboundFederationSend`, the restricted-room remote joins, and plausibly `TestKnocking` (which dies at `502 Failed to deliver invite to remote server` during setup). Under investigation; not diagnosed.
- **`soft_failed` is still never set.** Axon has only one auth check, against ancestor-resolved state, and no second check against current state from which a genuine soft-failure signal could be derived. Rejection is implemented; soft-failure is not.
- **`AuthRules` rule 2 is unimplemented** — an event's `auth_events` are never checked against what the selection algorithm would have chosen.
- **`TestEventAuth` remains genuinely un-root-caused.** The endpoint exists and returns a correct transitive chain; the field name matches the Go client's struct; the selection algorithm was traced and looked right. Recorded as UNKNOWN rather than given a plausible-sounding cause.
- The per-room summary field building is duplicated between `SpaceController` (per-user visibility) and `FederationController` (per-server visibility). The visibility rules genuinely differ; the field assembly does not, and belongs in a shared module.
- The `msc*` packages remain expected failures, unchanged since Phase 16.

## Phase 19 — Signatures over the redacted event: 35/89 → 46/89

Phase 18 ended at 35/89 with two systemic federation bugs open. Both are now closed, and closing the second one turned out to be the largest single correctness fix in the project's history. **46/89 (~52%)**, measured; `mix test` at **1137 tests, 0 failures**.

### The federation 401, in three parts

Outbound `make_join` was answered `401` by the peer, and — impossibly, given that every rejection path in `AxonWeb.Plug.FederationAuth` logs a warning — the *answering* server logged nothing at all. Three independent bugs:

- **`KeyServer` signed a `/_matrix/key/v2/server` document that didn't match what the controller serves.** The signature covered a document without `old_verify_keys`; the response includes it. Canonical-JSON signing is field-exact, so axon's own key self-signature failed verification for every real peer.
- **`/_matrix/key/v2/query` required X-Matrix auth.** It is the notary endpoint a server falls back to precisely when a direct key fetch fails validation — being unauthenticated is the entire point of it. And `FederationAuth`'s missing-header branch is the one path that *doesn't* log, which is why the answering side looked silent.
- **`RoomJoin` deduplicated imported state by `event_id`.** Room versions 3+ carry no `event_id` on the wire, so every such event keyed on `nil` and only the first survived — silently dropping `join_rules` and `power_levels` while still reporting a successful join.

### Signatures and event IDs were computed over the wrong thing

Axon signed events, verified signatures, and computed reference hashes over the **unredacted** event. The spec does all three over the **redacted** event. Verified against the reference implementation rather than inferred from spec prose (`gomatrixserverlib/eventcrypto.go`): signing at line 362 — *"Redact the event before signing so signature that will remain valid even if the event is redacted"* — verification at line 125, and `ReferenceSha256HashOfEvent` at line 297, *"the SHA-256 hash of the redacted event content"*.

The design reason is decisive: redaction must *preserve* signature validity, which is only possible if the signature never covered the stripped fields.

**Why nothing caught it for the project's entire history**: the mistake is self-consistent. Two axon servers both making it agree with each other perfectly. Axon↔axon federation worked, and all 1100+ local tests passed — including tests specifically written to exercise signature verification, because the test double (`FakeRemoteMatrixServer`) signed the same wrong way. It is only observable against a spec-compliant homeserver, where it breaks in both directions at once: their signatures fail here, and every event ID computed here disagrees with theirs for the same event.

There was no redaction algorithm anywhere in the codebase — only the `/redact` endpoint, which is a different thing.

Implemented as `AxonCrypto.Redaction`, with `room_version` threaded through `EventHash`, `KeyServer`, `EventBuilder`, `RoomJoin`, `RoomLeave`, `RoomKnock`, `Backfill`, `EventVerification` and both controllers (~30 call sites). The version parameter is **required, not defaulted** — a silently-wrong default is exactly the failure mode being removed. New `EventStore.get_room_version/2` serves the sites that only hold a `room_id`.

Cross-signing keys and third-party invite proofs are *not* room events: they get `EventHash.{sign_json,verify_json_signature}`, with no room version and no redaction, since redacting them would strip the very fields being attested. Kept as separate functions so the two can never be confused at a call site.

**The version cutoffs were wrong on the first attempt**, in four places, and every one of them was invisible locally because axon agreed with itself. Transcribed properly from `eventversion.go` (which maps each room version to one of five redaction algorithms) and `redactevent.go`: `join_rules.allow` is protected only from v8, `member.join_authorised_via_users_server` only from v9, `power_levels.invite` only from v11 — and an `m.room.server_acl` rule that had been *invented* and exists in no version at all. Top-level `origin`/`prev_state`/`membership` survive through v10 and are dropped at v11.

One deliberate, documented divergence: the spec text says `third_party_invite.signed` survives redaction of an `m.room.member` event, but it is absent from all five of the reference's keep-lists. Axon matches the reference, because matching it is what makes signatures agree with real peers — recorded in code rather than left as a silent difference.

Two existing tests asserted the old behaviour and were rewritten to pin the real property: a message body is redacted away *before* signing, so tampering with it cannot invalidate a signature — the old test asserted the exact opposite of the spec's intent. New tests pin both invariants directly: redaction preserves the event ID, and a signature still verifies after its event is redacted.

### What that one fix moved

Ten tests flipped to passing, most of them never targeted by anyone:

`TestEventAuth` (marked genuinely UNKNOWN by two separate investigations — the endpoint was correct all along, its *signatures* weren't), the whole `TestCannotSendNon{Join,Leave}Via…{V1,V2}` validation family, `TestClientSpacesSummary`, `TestClientSpacesSummaryJoinRules`, `TestContentMediaV1`, `TestMSC4291RoomIDAsHashOfCreateEvent_UpgradedRooms`, and `TestMSC4311FullCreateEventOnStrippedState`.

That vindicates an earlier worker's negative result, which is worth recording as a method note. Told to fix the `send_*` validation cluster, it traced every assertion to code that already satisfied it, found no bug, said so plainly, and reported that if those tests were still red the cause had to lie outside the file it owned. It was right. Overruling it would have meant rewriting correct validation code while the real bug sat in the signing layer.

### Other fixes this phase

- **Server names with an explicit port were parsed as the port.** 26 call sites across 8 files derived a Matrix ID's server with `id |> String.split(":") |> List.last()`, which returns `"8448"` for `@user:example.com:8448`. Every federation endpoint comparing that against the requesting origin rejected the request. Replaced by `AxonCore.MatrixId.server_name/1`, which splits on the *first* colon and returns `nil` for a room-v12 ID that has no server part by design.
- **Federated invites 500'd on the inviter's own request.** The countersigned event a remote returns has no `event_id` (v3+ wire format), and inserting it violated the events table's NOT NULL constraint. Same class as the `RoomJoin` dedup bug above.
- **`children_state` had no `ORDER BY`.** Postgres was free to return a space's child links in any order; clients and Complement both compare that array as an ordered sequence. Caught as a real regression between two Complement runs.
- **Room v12 could not be federation-joined at all** — the outbound `make_join ?ver=` list still advertised versions 2–11. And a v12 room's creator was refused their own upgrade, because `ensure_can_tombstone/2` read `power_levels.users` directly, where a v12 creator never appears by design.
- **Unmapped errors produced a 500 with no log line**, which reads like an unhandled exception and sends you hunting for a stacktrace that was never produced. `FallbackController`'s catch-all now logs the reason and route; that is what made the invite bug diagnosable. `send_join` likewise now distinguishes `bad_signature` / `missing_signature` / `key_not_found` instead of collapsing all three into "Bad event signature".

### Known gaps and honest caveats

- **`send_join` from Complement is still rejected with a genuine signature mismatch** — confirmed *not* a key-fetch failure via the new distinct error messages. The redaction now follows the reference algorithm key-for-key, so the remaining divergence is elsewhere in the signable bytes. Undiagnosed. This is what still gates `TestKnocking` and much of the restricted-rooms cluster.
- **Two tests regressed under memory pressure, not code**: `TestRestrictedRoomsRemoteJoinFailOver` and its MSC3787 variant, both 3-homeserver tests, failed with `pg_ctl: database system initialization failed` inside the container while the host had 8 of 9 GB of swap in use. This is the same environmental failure mode Phase 16 documented. Recorded as environmental with evidence rather than assumed either way.
- **`soft_failed` is still never set.** Rejection persistence landed; soft-failure needs a second auth check against current state that axon does not have.
- **`AuthRules` rule 2 remains unimplemented** — an event's `auth_events` are never checked against what the selection algorithm would have chosen.
- **The umbrella `mix test` cannot be run as one command reliably.** `FakeRemoteMatrixServer` binds fixed host ports, so cross-app runs collide and take down the `axon_web` application, producing a 528-of-529 failure cascade that looks catastrophic and is not. Per-app runs are green: 68 / 145 / 33 / 38 / 78 / 192 / 54 / 529.
- The host's PostgreSQL service remains down (PG18 binaries against a v16 data directory); all work this phase ran against a disposable pg16 container. Unresolved by choice — that data directory is shared with unrelated projects.

## Phase 20 — Redaction actually applied; csapi package 48/64 → 55/64

Phase 19 built the spec's redaction algorithm but only ever used it for signing/hashing. This phase found and fixed the gap that left open, plus five smaller csapi-package bugs, all against a real Complement run (not inferred from spec reading alone). Full `mix test`: **1145 tests, 0 failures**.

### `m.room.redaction` never touched the target event's stored content

`PUT /redact` built and stored the redaction event correctly but never looked up the event it named, so `GET /event`, `/messages`, `/context`, `/sync` and full-text search all kept serving the original, unredacted `content` forever. Fixed at write time in `EventStore.insert_event/2` (the one choke point both local sends and inbound federation share): on an `m.room.redaction`, look up the target, call `AxonCrypto.Redaction.redact/2` (built last phase, wired in for the first time here), and `UPDATE` the target's stored `content` — which also fixes search for free, since it queries live `content` with no separate index.

This closes over applying the redaction only when permitted — the redaction *event* is always accepted per spec, but *applying* it (stripping content) requires the redacter to be the target's original sender or hold the room's `redact` power level, which nothing checked before. An existing test had explicitly pinned the old, wrong "anyone can strip anyone's content" behavior as a documented gap; it now asserts the correct one.

### Other real bugs found and fixed this phase

- **`POST /join` silently dropped the client's request body.** Per spec it becomes the join event's content (a client-chosen `"foo": "bar"` should survive onto the stored `m.room.member` event) — caught by `TestRoomMembers`. Server-computed fields (`membership`, `join_authorised_via_users_server`) are merged on top so a client can't forge them.
- **`GET /messages` ignored `lazy_load_members` entirely**, always returning `"state": []`. Now returns the current `m.room.member` event for each sender present in the returned chunk.
- **`m.push_rules` never appeared in `/sync`.** It isn't a row in the generic `account_data` table, so nothing ever populated it; `AxonWeb.SyncHelpers` now computes it live from `AxonPush.UserRules` on every sync.
- **`GET /members` ignored `not_membership`** entirely (only the allow-list `membership` param was ever read).
- **Pushers survived a password change.** `logout_devices` (default `true`) logs out every device but the one making the request; a pusher belongs to a device, so one on a now-logged-out device should go with it. Now deleted; same-device pushers correctly survive.

### Infrastructure notes

- **The host's Postgres came back.** The v16-data-dir-vs-v18-binaries mismatch documented since Phase 16 was resolved by the machine owner at some point before this phase; `systemctl status postgresql` now shows a healthy PG 18.4 instance with `axon_dev`/`axon_test`/`axon_prod` intact. The disposable `axon-pg-dev` container is stale and unnecessary now — its port binding actively conflicts with the real service.
- **A prior Complement run's containers were found orphaned mid-session**, still consuming host RAM with no owning process — apparently left behind when a backgrounded `go test` invocation was interrupted before it could run its own cleanup. Cleared with `docker rm -f`; worth checking `docker ps -a | grep complement` for stragglers before assuming a memory reading reflects idle state.
- A single, solo `mix test` from the umbrella root works fine and gives the true 1145-test count — the "cannot be run as one command reliably" phrasing from Phase 19 describes *concurrent* runs colliding (`FakeRemoteMatrixServer`'s fixed ports), not a single run being broken. Scoping to one app via `cd apps/X && mix test` is its own trap: it starts only that app's OTP tree, not sibling apps' supervisors it depends on at runtime (`Axon.PubSub` is started by `axon_sync`, not `axon_core`), so an axon_core-only run crashes on any code path that broadcasts — looks like a regression, isn't.

### Where it stands, honestly

**csapi package: 55/64 (~86%), up from 48/64.** (89 was the whole-suite figure Phases 16-19 tracked, spanning `csapi` + top-level federation/room tests + `msc*`; this phase worked the `csapi` package specifically and didn't re-measure the full `./tests/...` figure.) Nine failures remain, each its own gap rather than one shared root cause:

- **`TestGetRoomMembersAtPoint` / `TestLeftRoomFixture`** — both need genuine point-in-time state resolution (room state/membership *as of* a given stream position or a user's leave point), which nothing in axon currently computes; `RoomMembership` only tracks current state. The larger of the deferred items — a real feature, not a quick fix.
- **`TestSearch`** — the redaction-related subtests now pass; two unrelated subtests (`Can_get_context_around_search_results`, `Can_back-paginate_search_results`) still fail — search result context/pagination, not investigated this phase.
- **`TestAsyncUpload`** — `POST /_matrix/media/v1/create` doesn't exist at all (MSC2246 async uploads).
- **`TestE2EKeyBackupReplaceRoomKeyRules`** — per-session key backup retrieval 404s where it shouldn't; not root-caused.
- **`TestDeviceListUpdates`, `TestPresence`** — timeouts waiting for federation/sync propagation; not root-caused.
- **`TestServerNotices`** — partly a genuinely-Synapse-specific admin API (`/_synapse/admin/v1/send_server_notice`) axon may never want to claim; not fully triaged apart from that.
- **`TestRoomImageRoundtrip`** — `EOF` on `createRoom` mid-test; didn't reproduce consistently, plausible host-memory-pressure flake in the same family Phase 16/19 documented, not confirmed either way.

Only the `csapi` package was run this phase — the top-level federation/room tests and `msc*` packages (the rest of the historical 89-test figure) weren't re-measured.

### Phase 20 continued — csapi package: 55/64 → 61/64

Kept going on the same phase after the redaction/media/search fixes above landed: worked through every remaining csapi failure with real Complement verification after each fix, not just local `mix test`.

- **`?set_presence=` on `/sync`** was read nowhere — `AxonSync.Presence.set_presence/3` was only ever called from `PUT /presence/{userId}/status`.
- **Server-notices bot user ID mismatch**: Complement hardcodes the recipient's expected inviter as `@_server:hs1` (Synapse's own `server_notices_mxid` convention); axon used `@server-notices:...`, so the invite-sender check could never match.
- **Server-notices room reuse never re-invited a recipient who'd left.** `ensure_room/1` returned the stored room_id unconditionally; a second notice after the recipient left landed in a room they weren't in anymore, with no invite to bring them back. Now checks current membership and re-invites when it isn't join/invite.
- **Key backup**: the single-session `PUT /room_keys/keys/{roomId}/{sessionId}` form (a bare body, no `rooms`/`sessions` wrapper) was silently a no-op — `do_put_backup_keys/4` only ever read `params["rooms"]`. Normalized all three PUT route shapes into one entry list. Also implemented the spec's actual replace rule (11.12.3.3) for the first time — is_verified=true beats false, then lower first_message_index, then lower forwarded_count — where axon previously overwrote unconditionally on every conflict.
- **Search**: page size was read from `event_context.limit` (not a real field — `event_context` only has before/after limits) instead of `filter.limit`; pagination (`next_batch`) didn't exist at all; and `events_before` was reversed into chronological order when spec (and the reference behavior) wants reverse-chronological, closest-match-first. Added an `offset`-based `next_batch` (not a keyset cursor — "rank" order has no single column to key off the way "recent" has `stream_ordering`) and fixed both bugs.
- **A real stuck-connection bug, not flakiness**: `TestAsyncUpload` failed with a bare `EOF` on the request *after* a successful upload — previously assumed to be the same class of host-memory-pressure flakiness Phase 16/19 documented for `TestRoomImageRoundtrip`. It wasn't. Both `MediaController` upload actions discarded the `conn` `Plug.Conn.read_body/2` returns (bound to `_conn`) and kept responding with the stale pre-read one; Plug's own docs are explicit that the returned conn must be threaded through, since Bandit tracks body-read state on it. Answering with the stale conn left the connection thinking the request body was never drained, so the *next* request pipelined on that same keep-alive connection stalled for exactly `read_body`'s default 15s `read_timeout` before Bandit gave up. The giveaway was the precision — three separate stalls, each within milliseconds of exactly 15.00s, too consistent to be memory-pressure jitter. Invisible in `Phoenix.ConnTest`, which never exercises a real transport. Fixing it also turned out to fix `TestRoomImageRoundtrip`'s EOF, which really was this same bug, not environmental flakiness as previously assumed.
- **`TestRoomImageRoundtrip`'s remaining failure** (after the above): `filter={"contains_url":true}` on `/messages` was read nowhere, so the filter was silently a no-op. Now filters the fetched page (not folded into the query, same documented trade-off as `lazy_load_members`).

Every fix above shipped with `mix test` regression coverage and was re-verified against a real Complement run, not just inferred from reading the fix. Full `mix test`: **1170 tests, 0 failures**.

### Where the csapi package stands, honestly

**61/64 (~95%)**, up from 48/64 at the start of this phase. Three failures remain:

- **`TestDeviceListUpdates`**: 9 of 10 subtests pass; the tenth (`when leaving a room with a remote user`) is the one Complement itself documents as unresolved even for Synapse — `runtime.SkipIf(t, runtime.Synapse) // FIXME: https://github.com/matrix-org/synapse/issues/13650`. Not chased further; the reference implementation doesn't reliably pass this either.
- **`TestLeftRoomFixture`** and **`TestGetRoomMembersAtPoint`** both need genuine point-in-time state resolution — room state/membership *as of* a given stream position, or as of a user's leave point — which nothing in axon computes today (`RoomMembership` only tracks current state, not history). This is a real feature, not a quick fix; explicitly deferred rather than attempted under time pressure.

The top-level federation/room tests and `msc*` packages (the rest of the historical 89-test figure) weren't run this phase.

## Phase 21 — Federation package triage: 46/88 → work in progress

Moved from the `csapi` package (Phase 20, now at 61/64) to the top-level federation-heavy `tests` package, which a fresh full run put at **46/88** before any fixes this phase.

### Fixed and verified via `mix test`

- **`createRoom`'s `is_direct` flag was documented and threaded through as an option but never actually used** — every initial invite's `m.room.member` content was hardcoded to `{"membership": "invite"}`, so an invitee had no way to know a room was meant as a DM before joining and seeing any other state. Now echoed onto each invite per spec.
- **`get_missing_events` never looked at `earliest_events`/`latest_events` at all** — it read a field (`known_ids`) that doesn't exist in the real request body, and just returned the room's most recent events by `stream_ordering`, regardless of what gap was actually asked about. Reimplemented as an actual backward walk via `prev_events`, stopping at (and excluding) `earliest_events` — the walk's natural visit order already produces the spec-required earliest-to-latest response order with no explicit reverse needed.

### Deep-dived and NOT where suspected: the `send_join` signature mismatch

Phase 19 left this open: `send_join` from a real Complement peer fails with `403 Bad event signature`, gating roughly a dozen tests (the whole knocking family, most of the restricted-rooms cluster). A full investigation this phase — instrumenting `EventVerification` to capture the exact received bytes from two real failing joins (room v7 and v10), then independently re-deriving the signable bytes with **three separate verifiers** (Go's `crypto/ed25519`, Python's `cryptography`, and `gomatrixserverlib`'s own `RedactEventJSON`+`VerifyJSON` — the actual functions the reference implementation uses internally) — found that **axon's computed signable bytes are byte-for-byte identical to the reference algorithm's**, and that **`gomatrixserverlib`'s own verifier, run directly against the same captured wire bytes, also rejects the signature**. That rules out any bug in `AxonCrypto.Redaction`/`EventHash`/`CanonicalJSON` — the mismatch predates axon's processing of the bytes entirely.

A full local reproduction harness (real `AxonWeb.FederationEndpoint` over real HTTPS, a Go client replicating Complement's exact `http.Transport`/TLS/ALPN construction, including a same-connection make_join-then-send_join sequence to rule out a Phase-20-style stale-`read_body` connection-state bug) reproduced the scenario cleanly every time — no failure outside the real Complement/Docker environment. Whatever corrupts or desyncs the request is specific to something in that containerized path — `host.docker.internal` NAT/port-forwarding, or an HTTP/2-vs-1.1 ALPN negotiation difference under the real Complement-issued cert — not axon's own logic.

**This line of investigation should not be reopened by re-suspecting the crypto layer** — that's now checked three independent ways. The next step, if pursued, is a network-level capture (tcpdump/mitmproxy) *inside* a live Complement run comparing what `gomatrixserverlib` actually puts on the wire against what Bandit/Plug actually parses, to find exactly where the two diverge. Not attempted this phase — it's a materially different (and slower) kind of debugging than anything else in this project's history, and was flagged back to the project owner rather than pursued further unilaterally.

### Still on the punch list, not yet investigated

The remaining ~40 failures span: the send_join-gated cluster above; auth-chain/rejection handling (`TestCorruptedAuthChain`, `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, `TestUnrejectRejectedEvents` — `soft_failed` is still never set, a known gap since Phase 19); `get_missing_events`'s gap-filling caller possibly still falling back to backfill unnecessarily in one scenario (plausibly downstream of the same signature investigation, since the events being verified there are also externally-signed); a remote-alias-resolution 404 (`TestFederationRedactSendsWithoutEvent`, also plausibly downstream); to-device/device-list-over-federation timeouts; MSC4297 state-res v2.1; and several one-off endpoint/behavior gaps (`TestJumpToDateEndpoint`, `TestFederationKeyUploadQuery`, `TestInboundFederationProfile`, `TestRemoteAliasRequestsUnderstandUnicode`, `TestOutboundFederationEventSizeGetMissingEvents`'s state_key-length validation). Full `mix test`: **1173 tests, 0 failures** as of the last commit this phase.

### The send_join signature mismatch — pushed further, still not axon's bug

Per owner request, went beyond Phase 21's investigation: captured the **truly raw, pre-JSON-parse request body bytes** for a live failing `send_join` (via a temporary `Plug.Parsers` `body_reader` hook — functionally equivalent to a network capture, since it's the literal bytes Bandit received off the TLS-terminated socket, with zero ambiguity about transport-level mangling) from a real `TestKnocking` run (room v7), then independently re-verified from scratch in Python (`cryptography` library, hand-rolled Matrix canonical JSON, the redaction keep-list transcribed a third time directly from `gomatrixserverlib`'s `redactevent.go`/`eventversion.go` source rather than trusted from memory) — **the signature still does not verify**, confirming Phase 21's finding a third independent way and closing the "maybe it's a JSON-decode-fidelity bug in axon's own Jason parsing" gap that first investigation had left open.

Traced further into *how* this specific event gets built: `TestKnocking` uses Complement's own `federation.Server.MustJoinRoom` (not a second axon instance) — it calls axon's `/make_join` to get a join-event template, feeds it through `gomatrixserverlib`'s `EventBuilder.Build` (a path with special-casing for pseudo-ID/MSC4???-style room versions), and signs the result. Complement's `go.mod` pins `gomatrixserverlib` at a `v0.0.0-<timestamp>-<hash>` **pseudo-version** — an unstable dev snapshot, not a tagged release. Combined with three independent, from-scratch verifications all agreeing axon computes the objectively correct canonical bytes for what it receives, the balance of evidence now points at a bug in that pinned dev dependency's event-building/signing path, not at axon.

**Recommendation: stop re-investigating this as an axon bug.** Any further progress needs either an upstream report against that `gomatrixserverlib` commit, or pinning/bisecting Complement's dependency to see if a different version resolves it — both outside this project's own code. Debug instrumentation used for this (a temporary `body_reader` capture in `federation_endpoint.ex`) was fully reverted; `git status` is clean.

## Phase 21 continued — federation package: 46/88 → 48/88

Kept going with real, individually-verified fixes after the send_join investigation above:

- **`GET /_matrix/federation/v1/query/profile`** now validates the `user_id`'s server-name syntax before looking anything up — `@user:localhost:http` (non-numeric port) fell through to a bare 404 instead of the 400 the spec requires. New `AxonCore.MatrixId.valid_server_name?/1`.
- **`GET /_matrix/client/v3/directory/room/{roomAlias}`** only ever answered for aliases hosted locally — any other server's alias 404'd unconditionally instead of asking that server via federation `query/directory`, the same lookup `RoomController`'s join flow already had. This is a real, previously-entirely-missing feature, confirmed working for the ordinary case; one exotic subtest (`TestRemoteAliasRequestsUnderstandUnicode`, a `#` as the very first byte of the alias combined with multi-byte UTF-8) still fails after this fix and wasn't fully root-caused — extensive local verification confirmed axon's own encode/decode/DB-lookup is byte-exact at the path-parameter, query-string, and canonical-JSON-signing layers; the residual gap is most likely in exactly how the Go test client serializes a URL path segment starting with `#`, not resolvable without a live wire capture of the request Complement's client actually sends.
- **`keys/claim`** (both the client and federation-inbound handlers) always included every requested `user_id` in the response, with an empty device map when nothing was actually claimable, instead of omitting the user entirely — a client can't tell "exhausted" apart from "here's an empty map for you" that way. Verified: this specific bug is fixed (`TestFederationKeyUploadQuery`'s failure moved from the claim assertion to a later, unrelated device-list-over-federation sync timeout — real progress even though the test as a whole still fails).

Full `mix test`: **1178 tests, 0 failures**.

### Where the federation package stands, honestly

**48/88**, up from 46/88 at the start of this phase (two tests flipped clean: `TestIsDirectFlagLocal`, `TestInboundFederationProfile`; several others moved their failure point downstream without going fully green, as noted above). The remaining ~40 failures are dominated by two things that were investigated and explicitly **not** fixed this phase:

1. **The send_join signature mismatch** (see above) — gates the whole knocking family and most of the restricted-rooms cluster (~13 tests). Conclusively ruled out of axon's own code across three independent verifications; likely a bug in Complement's pinned dev-snapshot `gomatrixserverlib` dependency, not something to keep re-investigating here.
2. **`soft_failed` is still never set** (open since Phase 19) — plausibly gates the auth-chain/rejection cluster (`TestCorruptedAuthChain` — confirmed itself gated by the send_join bug specifically; `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, `TestUnrejectRejectedEvents` — not yet individually root-caused this phase).

Everything else in the remaining ~40 (to-device/device-list-over-federation timeouts, MSC4297 state-res v2.1, several one-off endpoint gaps) was not investigated this phase. The `csapi` package (Phase 20, separately at 61/64) and `msc*` packages were not re-run.

## Phase 22 — federation package: 48/88 → 53/88

Two real, independently-verified bugs found and fixed, both by tracing actual 500s/404s rather than guessing from Complement's pass/fail summary alone.

- **Inbound federation invite (`PUT /_matrix/federation/v2/invite/...`) 500'd on the event-shape every real inviting server actually sends.** `accept_invite/4` signed the incoming event and inserted it without ever computing `event_id` — room versions 3+ never carry one on the wire (it's the reference hash), so `EventStore.insert_event/2` got a nil `event_id`, the changeset's `NOT NULL` violation fell through `FallbackController`'s catch-all, and the inviting server saw an opaque `500 M_UNKNOWN`. Same event_id-backfill pattern `AxonFederation.RoomJoin` already used for auth-chain storage, just missing here. This gated most invite-related federation tests, not only the `is_direct` one it was first spotted on.
- **Every federated alias lookup (`GET /_matrix/federation/v1/query/directory?room_alias=...`) silently sent an empty `room_alias`.** Both call sites (`RoomController`'s join/knock alias resolution, `DirectoryController`'s remote alias GET) built the query string with `URI.encode/1`, which leaves `#` unescaped — and every Matrix alias starts with `#`. `Finch.build/5` re-parses the URL string with `URI.parse/1` before sending, and per RFC 3986 an unescaped `#` starts the fragment, which is never transmitted — so the alias never reached the wire, only `room_alias=` did. `FakeRemoteMatrixServer.put_response/4`'s regex-only path matching let this slip past axon's own test suite entirely (it never inspected the query string it actually received, so a canned 200 always looked like success). Fixed by switching to `URI.encode_www_form/1`, which correctly percent-encodes `#`; both regression tests now assert on `FakeRemoteMatrixServer.requests/1`'s recorded query string, not just the mocked response.

**Verified via a clean before/after Complement diff** (identical image rebuild, same commit, full package re-run both times): exactly 5 tests flipped from fail to pass, zero regressions — `TestIsDirectFlagFederation`, `TestOutboundFederationSend`, `TestFederationRedactSendsWithoutEvent`, `TestJoinFederatedRoomWithUnverifiableEvents`, and `TestRemoteAliasRequestsUnderstandUnicode`. That last one closes a gap Phase 21 left open and had misattributed to "exactly how the Go test client serializes a URL path segment starting with `#`" — same character, same root cause, wrong call site (it's the *query string* built for the federation-side lookup, not path serialization on the client side).

### A third lead — traced to its root cause after all

`TestNetworkPartitionOrdering` (and by shape, plausibly `TestGetMissingEventsGapFilling`, `TestComplementCanCreateValidV12Rooms`, `TestSyncOmitsStateChangeOnFilteredEvents`) all shared a symptom: a remote server creates an event early, transmits it late (after the local head has advanced several generations via unrelated events), and axon appeared to never apply it.

Two independent local reproductions of the exact PDU shape (single `prev_events` pointing at a known-but-stale ancestor) — one calling `AxonRoom.RoomProcess.apply_remote_event/2` directly, one over real HTTP through `PUT send_transaction/2` — both accepted and applied the event cleanly, which at the time looked like the same kind of dead end as the `send_join` signature mismatch (not reproducible outside the real Complement/Docker path). It wasn't: both reproductions passed because they followed the rest of axon's own test suite's convention of hand-supplying a fake `"event_id"` on the test PDU — a real wire PDU for room versions 3+ never carries one. Rewriting both reproductions to omit it (matching what Complement/gomatrixserverlib actually send) immediately reproduced a real bug:

- **`PUT /_matrix/federation/v1/send/{txnId}` computed each PDU's `event_id` (`pdu["event_id"] || compute_event_id(pdu)`) only to key the per-PDU response map — it never attached that id back onto the `pdu` map before passing it on to `process_inbound_pdu/2`.** Every inbound federated event for a room version that omits `event_id` from the wire (3 through 12 — i.e. nearly everything) flowed through `AxonFederation.Backfill` and `AxonRoom.RoomProcess` still missing it, hit `AxonCore.EventStore.insert_event/2`'s `NOT NULL` violation, and was silently dropped — the transaction still 200'd (per-PDU errors aren't surfaced as an HTTP failure) with no trace anywhere in the response.
- **`AxonFederation.Backfill` had two of its own instances of the same gap.** `fetch_get_missing_events/3` sent a literal `"latest_events": [null]` for the gap-triggering PDU — caught directly by `TestOutboundFederationEventSizeGetMissingEvents`'s strict request-shape assertion ("got latest_events [null] want [$real_id]"). `apply_oldest_first/3` deduplicated *fetched* events by `event_id` before ever computing one, so a batch of same-room-version fetched events could silently collapse down to whichever happened to sort first.

Fixed all three call sites with the same computed-or-derived `event_id` pattern already used by `AxonFederation.RoomJoin` and this phase's invite fix. Both regression tests from the "not confirmed" investigation above were rewritten to drop the fake `event_id` and now correctly exercise (and catch) the real bug.

**Verified via a second before/after Complement diff**, same methodology as the first: 5 more tests flipped clean, zero regressions — `TestComplementCanCreateValidV12Rooms`, `TestGetMissingEventsGapFilling`, `TestOutboundFederationEventSizeGetMissingEvents`, `TestSyncOmitsStateChangeOnFilteredEvents`, `TestUnrejectRejectedEvents`.

`TestNetworkPartitionOrdering` itself still fails, but got meaningfully further — the forked event now applies correctly (its content shows up in the room). The remaining failure is narrower and different in kind: `GET /rooms/{roomId}/messages?dir=b` backward pagination returns the late-arriving forked event out of order relative to its topological (depth) position, because pagination is presumably ordered by `stream_ordering` (insertion order) rather than the DAG's topological order — exactly the mismatch a late-arriving-but-early-in-the-DAG event exposes. Not investigated further this phase; a concrete, scoped lead for next time (look at whatever backs `GET .../messages`' `dir=b` ordering).

### Two more fixes: a spec-backwards preset default, and a real test-infrastructure bug

While investigating `TestInboundCanReturnMissingEvents` (its `get_missing_events` response ordering didn't match the test's expected event sequence), the actual room-state cause turned out to be unrelated to ordering: **`guest_can_join` was backwards from spec for every preset.** Per the createRoom preset table, `public_chat` is the one preset guests can NOT join by default, and both private presets allow them — axon had this exactly inverted, and its own preset unit tests and an E2E guest-access test had both independently locked in the same wrong values rather than being checked against spec. Fixed in `AxonRoom.CreateRoom.preset_values/1`, both axon tests corrected. This does **not** fully resolve `TestInboundCanReturnMissingEvents` — that test's actual blocker is structural (see below), not this value — but it's a real, independently-worth-having correctness fix.

Separately, while re-verifying nothing regressed, `mix test` **stopped being reliably green on a solo run** — 4 of 5 consecutive solo runs cascaded into "440-ish of ~567 axon_web tests red, ETS table missing," previously understood (see the `test-suite-not-concurrency-safe` memory) as *only* a symptom of two concurrent `mix test` runs colliding on `FakeRemoteMatrixServer`'s fixed ports. It turned out to have a second, independent cause: `AxonFederation.OutboundQueue`'s periodic `:sweep` and `AxonWeb.FederationFanout`'s three PubSub handlers all touch `Repo` from a long-lived process that outlives any single test's Ecto sandbox ownership — a tick/message landing in the gap between two tests raised an uncaught `DBConnection.OwnershipError`, crashed the GenServer, and repeated crashes exhausted the supervisor's restart intensity, taking the whole application down with it. `AxonFederation.DeviceListFanout` already had a guard for exactly this; these two didn't. Fixed with the same guard; confirmed 5/5 clean solo runs afterward. Purely a test-environment fix — this error can't occur outside the sandbox.

### One more relay gap, same shape as send_join/leave/knock

`federate_invite/4` (the sending side of a cross-server invite, `RoomController`) applied the invitee's-server-countersigned invite event via `RoomProcess.apply_remote_event/2` with no `relay_exclude` — so like the send_join/leave/knock case fixed earlier, it only ever reached this server's own DB/sync, never any *other* server already resident in the room. Fixed the same way (`relay_exclude: target_server`), with a new regression test (`federated_invite_test.exs`) verifying an already-resident third server actually receives the relayed invite via `FakeRemoteMatrixServer.requests/1`, not just a synchronous assertion (delivery is async — `AxonFederation.OutboundQueue` spawns a Task — an earlier draft of this test raced itself and had to be rewritten to poll).

### Application services never worked at all under Complement

`TestJoinFederatedRoomFromApplicationServiceBridgeUser` gave an unambiguous signal — a bridge user's federated room join 401'd with `M_UNKNOWN_TOKEN` outright. Root cause was two compounding, previously-invisible gaps:

1. Complement copies one Synapse-style registration YAML per configured application service into every homeserver container it deploys, at a fixed path (`/complement/appservice/<id>.yaml`) — axon only ever read its own hand-authored `appservices.json`, so these files existed on disk but nothing loaded them. New `AxonWeb.AppService.RegistrationYaml` is a narrow reader for that one fixed, machine-generated shape (deliberately not a general YAML parser — see its moduledoc), wired into `AppService.Manager` alongside the existing JSON loader. `complement/start.sh` now exports `AXON_APPSERVICE_DIR`; `config/runtime.exs` reads it into a new `:axon_web, :appservice_dir` config (absent outside Complement, same tolerance the JSON loader already had for a missing file).
2. Even with a registration loaded, nothing accepted an `as_token` as a credential — `AppService.Manager.verify_as_token/1` existed but was called from nowhere. `AuthenticateToken` now falls back to it after local-token and OIDC both fail: authenticates as the appservice's own `sender_localpart` user by default, or as any user matching one of its registered namespaces via `?user_id=` impersonation (both spec-required), provisioning the target user on first use via new `AxonCore.UserStore.authenticate_via_appservice/3` (mirroring the existing OIDC user-provisioning pattern — an appservice's ghost users never go through `/register`).

While in this area, also implemented `?ts=` timestamp massaging (`PUT .../send/:eventType/:txnId` never read it at all — Phoenix merges query and path params into one map, so it silently leaked into the sent event's own *content* instead of being applied to `origin_server_ts`), gated to appservices only per spec via a new `is_appservice` assign `AuthenticateToken` now sets.

**Verified**: `TestJoinFederatedRoomFromApplicationServiceBridgeUser` flipped clean. `TestJumpToDateEndpoint`'s appservice-driven "same timestamp" subtests need both halves of this (an appservice that can authenticate *and* whose `?ts=` is honored) — not yet reverified after the `?ts=` piece landed second; see below, still failing as of the last full run but for the separately-root-caused reason.

### A silent extra backfill call — real fix, but not this test's actual blocker

While chasing `TestInboundCanReturnMissingEvents`'s `guest_access` red herring (see below), found `TestOutboundFederationIgnoresMissingEventWithBadJSONForRoomVersion6` failing on an unexpected `/backfill` call. `AxonFederation.Backfill.close_gap/4`'s catch-all fell back to `/backfill` whenever `get_missing_events` returned a well-formed 200 with no events — indistinguishable, in the old code, from the request actually failing. Fixed by only falling back to backfill on a genuine failure (see commit); this is independently correct and has its own regression coverage now, but **re-running against live Complement after the fix, this exact test still fails on the same unexpected `/backfill` call** — so it wasn't (or wasn't only) this test's actual blocker.

Refined finding: the unexpected backfill call happens on the test's *first* transaction (the one where `get_missing_events`'s stubbed response returns a single event with a float value in its content, deliberately built as a room-v5-shaped event to bypass gomatrixserverlib's own float rejection client-side). `close_gap`'s first branch (`{:ok, events} when events != []`) is the one taken — the response isn't empty — applies the fetched event via `apply_oldest_first`/`verify_and_apply`, then re-checks `missing_ids(missing)` against the *original* missing set; if that still shows the id as missing, it falls back to backfill for "whatever's still unresolved." For that id to still read as missing after `apply_oldest_first`, the float-content event must never have gotten a DB row at all — not even a rejected one (`EventStore.get_event_maps/1` doesn't filter by rejected status, so a normally-rejected event would already read as "no longer missing"). That points at `verify_and_apply/3` either raising (not returning a clean `{:error, reason}` `reject_and_persist` could catch) somewhere in signature verification or canonical-JSON encoding when it hits the float, or some other silent no-op — not confirmed which. Next step: reproduce locally with `AxonCrypto.CanonicalJSON.encode_to_binary/1` (or wherever float content actually gets handled) fed a float-containing event map directly, outside the full federation pipeline, to see whether it raises. Not attempted this phase — the live-Complement-only reproduction budget for this investigation was already spent on the (still valuable) fix above.

### Where the federation package stands, honestly

**61/88**, up from 48/88 at the start of this phase — confirmed with the backfill fix included (a same-count run: `TestOutboundFederationIgnoresMissingEventWithBadJSONForRoomVersion6` didn't flip as hoped per the refined finding above, but `TestMSC4289PrivilegedRoomCreators` flipped clean instead, net even). Verified via before/after Complement diffs at every step, zero regressions except transient `send_join`-signature-mismatch flakiness (`TestCannotSendNonKnockViaSendKnock`/`TestCannotSendKnockViaSendKnockInMSC3787Room` each flipped both directions across consecutive runs untouched by any of this phase's changes — the same already-documented non-deterministic dev-dependency issue, not a regression). The remaining failures are still dominated by the same two things Phase 21 identified:

1. **The send_join signature mismatch** — still gates the whole knocking family and most of the restricted-rooms cluster, and confirmed once more this phase: `TestCorruptedAuthChain` fails at exactly `send_join => 403 Bad event signature`, same signature as every other test in this cluster. Not axon's bug (Phase 21).
2. **Transitive auth-event rejection doesn't actually trigger in `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, despite `RoomProcess`'s `any_rejected?`/`unknown_ids` cond clauses existing specifically for this.** Re-checked this phase now that the event_id-attachment fix is in — it wasn't the blocker here. The outlier event itself is correctly unreachable (`GET .../event/{outlierId}` 404s, as expected). But the two regular events that list the outlier in their own `auth_events` are wrongly **accepted** (`GET .../event/{id}` returns 200, test wants 404) instead of being transitively rejected. Not root-caused this phase — worth checking next: whether the outlier's event_id as axon computed/stored it actually matches the id gomatrixserverlib put in the *other* events' `auth_events` list (a mismatch there would make `any_rejected?`/`unknown_ids` look up the wrong key entirely and silently pass both checks). `soft_failed` still being unimplemented is a separate, still-open gap but isn't obviously this test's blocker specifically.

**`TestInboundCanReturnMissingEvents`'s real blocker (all 4 subtests)**: axon's `send_initial_events/7` unconditionally emits an `m.room.guest_access` state event as the 5th event in every new room, but the test's expected event sequence (member, power_levels, join_rules, history_visibility ×1-2, message) has no slot for it at all — every event after that point comes back shifted one position out of what the test expects. Not fixed this phase — removing the event outright isn't obviously correct either (would need to confirm real Synapse never sends an explicit initial guest_access event for these presets, not just that this one test's hardcoded list omits it).

**`TestJumpToDateEndpoint`'s real blocker, root-caused but not fixed**: `AxonCore.EventStore.find_event_by_timestamp/3` only ever looks at local history, and `AxonWeb.EventController.timestamp_to_event/2`'s `cond` accepts whatever it finds unconditionally — there's a documented federation fallback (`AxonFederation.TimestampToEvent`) for exactly the case where local history doesn't reach far enough, but nothing decides *when* to actually use it. Confirmed directly: a server that joined a room late (so its own history starts at the join, missing everything from before) queried for the earliest event at-or-after a timestamp from *before* it joined, and got back its own join event instead of the real answer (an older message that only exists on the room's other, longer-resident server) — the local query happily returns "some" locally-known event that technically satisfies the `>=`/`<=` comparison without ever recognizing that its own local history might not reach back far enough to contain the true nearest event. Fixing this properly needs a real heuristic (e.g. Synapse's approach: only trust a local answer if it isn't immediately adjacent to this server's own earliest known event in that room, otherwise ask federation) — genuine feature work, not a one-liner, so not attempted this phase.

Also untouched: to-device/device-list-over-federation timeouts, MSC4297 state-res v2.1 (2 tests), `TestMSC4291RoomIDAsHashOfCreateEvent_AuthEventsOmitsCreateEvent` (v12 domain-less room IDs tripping something in Complement's own pinned `gomatrixserverlib` snapshot's `eventauth` package — smells like the same class of dev-dependency issue as send_join), and the `/messages` topological-ordering lead from `TestNetworkPartitionOrdering` above.

Full `mix test`: **1204 tests, 0 failures** (reliably — see the test-infrastructure fix above).

## Phase 23 — Five targeted fixes, mostly closing Phase 22's own punch list (mix-test-verified only, Complement not re-run)

Five independent, sequentially-committed fixes. Four directly close gaps Phase 22 root-caused but explicitly left unfixed; the fifth (registration gating) is new, unrelated hardening work found while doing the writeup pass. **This phase did not run Complement** — everything below is verified against `mix test` and new regression tests only. Pass-rate deltas are stated as expectations tied to the specific tests each fix targets, not confirmed numbers — see "Where this leaves things" below.

Final regression check, full umbrella, solo run (never run `mix test` concurrently with anything else in this repo — see the fixed-port `FakeRemoteMatrixServer`/Ecto-sandbox-ownership notes in prior phases): **1,233 tests + 15 doctests, 0 failures**, clean across all 8 apps (`axon_crypto` 68, `axon_core` 152, `axon_sync` 33, `axon_media` 42, `axon_push` 78, `axon_room` 198, `axon_federation` 54, `axon_web` 608 — each reported separately since this umbrella's `mix test` runs and summarizes each app in sequence rather than one pooled total).

- **`bbec0fe` — Registration is now gated behind a real `registration_enabled` flag, off by default in prod.** Previously `POST /register` was wide open (rate-limited only) on any deployment that wasn't using OIDC — unlike Synapse, which defaults `enable_registration: false`. New `config :axon_web, :registration, enabled:` (read from `REGISTRATION_ENABLED` in `config/runtime.exs`), off in `config/config.exs`'s base config, on in `config/dev.exs` and `config/test.exs` so local dev and the existing suite are unaffected. When disabled and OIDC isn't handling registration instead, `register/2`/`register_available/2` reject with the same `403 M_FORBIDDEN "Registration has been disabled"` shape Synapse returns; `_synapse/admin/v1/register` (shared-secret admin registration) is untouched either way. **This is a behavior change anyone deploying axon publicly needs to know about**: a server upgraded to this commit with no `REGISTRATION_ENABLED=true` in its environment will silently stop accepting new self-registrations — worth calling out in release notes, not just this roadmap entry. Verified directly (not just inferred) that `complement/start.sh` was updated in the same commit to `export REGISTRATION_ENABLED=true` alongside its existing `RELAXED_RATE_LIMITS` override — Complement creates every test user via plain `POST /register`, so without this the whole suite would fail at user creation, not just whatever test happens to touch registration directly. Confirmed wired correctly by reading `complement/start.sh` and `config/runtime.exs` directly; **no blocker here**.
- **`23223ae` — `m.room.guest_access` is no longer auto-emitted for presets that don't call for it.** Synapse only auto-sends an initial `guest_access` event when the preset's own `guest_can_join` is true (`private_chat`/`trusted_private_chat`); `public_chat` gets no explicit event at all and falls back to the implicit "forbidden" default. `send_initial_events/7` previously emitted the event unconditionally for every preset — the exact cause Phase 22 root-caused for **`TestInboundCanReturnMissingEvents`** (all 4 subtests): the extra event shifted every subsequent event in the room's expected sequence by one position. `preset_values/1` split into join_rule/history_visibility plus a new `guest_can_join?/1` predicate; the auto event is now skipped both when the preset doesn't call for it and when the client's own `initial_state` already supplied one (`has_explicit_guest_access?/1`). Existing readers of `m.room.guest_access` already default missing state to "forbidden," so effective guest-join behavior for `public_chat` is unchanged — this only removes the redundant explicit event. Expected to flip `TestInboundCanReturnMissingEvents`'s 4 subtests, unconfirmed against live Complement.
- **`0d3a448` — `GET /messages?dir=b` now orders by depth (with `stream_ordering` as a same-depth tiebreak), not `stream_ordering` alone.** The old ordering reflected local DB-insert time rather than DAG causality, so a remote event created early but transmitted/inserted late sorted near the top of a backward page despite topologically belonging much further back — exactly the residual failure Phase 22 left open for **`TestNetworkPartitionOrdering`** after fixing that test's actual forked-event-application bug. The forward (`dir=f`) branch and the `stream_ordering`-based windowing/limit bound are unchanged; only backward-pagination ordering moved to depth-first, mirroring Synapse's topological+stream ordering. Expected to close `TestNetworkPartitionOrdering` fully, unconfirmed against live Complement.
- **`61070db` — Client-facing `GET /rooms/{roomId}/event/{eventId}` now treats rejected/soft-failed events as not-found.** `EventStore.get_event/1` deliberately has no rejected/soft_failed filter (federation's own `GET /event/{id}` and auth-chain walks need to keep seeing rejected events), but `EventController` was reusing that same unfiltered lookup for the *client* API — so an event `RoomProcess` had correctly rejected, including the transitive case (a legitimate event whose `auth_events` reference an already-rejected or unknown ancestor via `any_rejected?`/`unknown_ids`), was still served back to clients with a 200. New `EventStore.get_visible_event/1` is used from the client controller only; `federation_controller.ex` is untouched. This directly targets the exact gap Phase 22's investigation of **`TestInboundFederationRejectsEventsWithRejectedAuthEvents`** landed on: the transitive-rejection logic itself (`any_rejected?`/`unknown_ids`) was already correct and the outlier event was already correctly unreachable — the two events that referenced it in their own `auth_events` were the ones wrongly returning 200 instead of 404, which is precisely what this fix closes. Note this does **not** implement `soft_failed`-setting itself (still a separate, open gap per Phase 21/22) — it only makes the client API honor rejection status that's already being computed. Expected to flip `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, unconfirmed against live Complement.
- **`55e4f77` — `timestamp_to_event` (`GET /_matrix/client/v1/rooms/{roomId}/timestamp_to_event`) now distrusts a local answer that sits at the edge of this server's own known history**, instead of accepting whatever `find_event_by_timestamp/3` returns unconditionally. New `EventStore.trustworthy_local_timestamp_answer?/2` matches Synapse's heuristic: a local answer is untrustworthy if it's this server's own earliest (or latest, depending on direction) known event in the room — the case a server that joined late and is asked about a moment before it joined can't actually answer correctly from local data alone. An untrustworthy answer now falls back to the already-implemented-but-previously-dead-here `AxonFederation.TimestampToEvent`, with the untrustworthy local answer used only as a last resort if federation also comes back empty. This is exactly the heuristic Phase 22 root-caused as **`TestJumpToDateEndpoint`'s** real blocker and explicitly deferred as "genuine feature work, not a one-liner." Expected to help `TestJumpToDateEndpoint`'s timestamp-boundary subtests; the same test also has an appservice-`?ts=`-dependent subtest from Phase 22's application-services work, so a full flip isn't guaranteed on this fix alone. Unconfirmed against live Complement.

### Where this leaves things

All five fixes have dedicated new regression tests (`auth_controller_test.exs`, `create_room_test.exs`, `event_store_test.exs` ×2, `event_controller_test.exs`, `timestamp_to_event_test.exs`) and a clean full `mix test` run — see the top-level count above. **None of this has been checked against a live Complement run this phase.** Four of the five fixes target specific, previously-identified Complement tests (`TestInboundCanReturnMissingEvents`, `TestNetworkPartitionOrdering`, `TestInboundFederationRejectsEventsWithRejectedAuthEvents`, `TestJumpToDateEndpoint`) and *should* move the federation package's count up from Phase 22's confirmed 61/88, but by how much — and whether any of these mix-test-verified fixes behave differently against the real `gomatrixserverlib`-driven traffic Complement generates, the way past phases' "should be fixed" guesses have occasionally not panned out cleanly — is not known until that run happens. **A full before/after Complement diff against Phase 22's baseline is the clear next step**, using the same methodology (`tests/csapi` and `tests/` packages, identical image rebuild, same commit) prior phases used to get real numbers rather than assumed ones.

## Phase 24 — The point-in-time feature from Phase 22/commit 932c2af had two real cracks in it, not zero

A live Complement run (the "next step" Phase 23 called for) found `TestGetRoomMembersAtPoint` and all 5 subtests of `TestLeftRoomFixture` still failing — despite `EventStore.get_room_members_at/3` and the `can_access_event?/3` leave/ban case (commit 932c2af) and the later `visibility_bounds`/`event_visible?` extraction to cover `/messages` too (`efa7805`) both already existing on `master`. Neither commit was reverted or wrong on its own terms; each left a specific edge the other didn't cover. Root-caused three independent gaps, all downstream of the same theme (a departed member's, or a point-in-time query's, view of a room isn't actually frozen everywhere it needs to be):

- **A room's own `timeline.prev_batch` was hardcoded to `"0"` whenever that sync response wasn't `limited`** (`SyncController.build_initial_room_data/4` and `build_incremental_room_data/3`) — which every small, non-truncated room's sync always is, since axon's default timeline limit (100) comfortably covers a freshly-created test room. `at=0` then resolves to "before this room's very first event" for `EventStore.get_room_members_at/3`'s `<=` query, so `GET /members?at=<that room's own prev_batch>` came back an empty chunk instead of the membership actually visible in that response — exactly `TestGetRoomMembersAtPoint`'s failure. (The existing `members?at=<token>` regression test from 932c2af used the top-level `next_batch` instead of a room's `timeline.prev_batch`, which never exercises this hardcoded branch — that's why it didn't already catch this.) Fixed by pointing a non-limited response's `prev` at the *last* delivered event's own ordering rather than `"0"` — inclusive of everything the response actually showed, exclusive of anything after (e.g. a later join), which is exactly what `get_room_members_at/3`'s `<=` semantics need.
- **`GET /members` with no `at` always answered with current, live membership, even for a requester who has left or been banned.** Per spec, a departed requester with no explicit `at` should get membership *as of when they left*, not the room's live roster — otherwise they see anyone who joined after they left (`members_for_a_departed_room`'s exact failure: a departed user's own `/members` call returned a join-state entry for a user who joined after they left). Fixed in `RoomController.members/2` by defaulting to the requester's own `leave_ordering` (from `EventController.visibility_bounds/2` — the same boundary `/event/{id}` and `/messages` already cap a departed member's own visibility at) whenever `at` is absent and the requester's current membership is `"leave"`/`"ban"`.
- **`GET /rooms/:id/state` and `/state/:type/:state_key` always read `RoomProcess`'s live in-memory state**, with no departed-member routing at all — so a member who left before some later state change (a room name change, in `TestLeftRoomFixture`'s fixture) saw the *current* value, not the value as of when they left (`state_for_a_departed_room` and `'m.room.name'_state_for_a_departed_room`'s failures). New `EventStore.get_room_state_at/2` and `get_state_event_at/4` generalize `get_room_members_at/3`'s DISTINCT-ON-latest-row-per-group pattern from `m.room.member` alone to any `(type, state_key)`; both `EventController.get_state/2` and `get_state_event/2` now route a departed requester (again via `visibility_bounds/2`'s `leave_ordering`) through the point-in-time query instead of `RoomProcess`.
- **`EventStore.get_messages/4`'s `dir=b` query is exclusive of its `from_ordering` boundary by design** (see `event_store_test.exs`'s existing `stream_ordering + 1` convention for deliberately including a specific event) — and a departed member's own leave/ban event is very often exactly that boundary: a `from` token lifted from that same user's own `/sync` `next_batch` taken right after leaving carries their leave event's own `stream_ordering` verbatim, since that was the newest event ever delivered to them. Left as-is, the exclusive boundary silently dropped that one legitimate, already-seen event off a backward page (`messages_for_a_departed_room`'s failure: the chunk was missing the requester's own leave `m.room.member` event). Fixed narrowly in `EventController.get_messages/2` — not by loosening the shared `EventStore.get_messages/4` query itself, which every other caller (including the codebase's own `+1`-to-include convention) relies on staying exclusive — by nudging `from_ordering` out by exactly one *only* when the requester has departed, is paging backward, and the boundary they supplied sits at-or-before their own `leave_ordering`. `event_visible?/2`'s existing cap still independently bounds every result at `leave_ordering`, so the nudge only ever admits their own already-visible leave event to the raw fetch, never anything past it.

New regression coverage: two tests in `room_controller_test.exs` (a room-scoped `prev_batch` fed to `members?at=`, and a departed requester's `?at`-less `/members`), three in `event_controller_test.exs` (departed-member `get_state_event/2`, `get_state/2`, and the `get_messages/2` boundary case) — all four exercised end-to-end through the real HTTP pipeline, mirroring Complement's own request shapes rather than calling `EventStore`/`RoomProcess` functions directly.

**Verified directly against live Complement**: both `TestGetRoomMembersAtPoint` and `TestLeftRoomFixture` (all 5 subtests) flip clean from fail to pass, run individually with `-v` against a freshly rebuilt image containing this phase's fix. That moves `tests/csapi` from Phase 22's confirmed 61/64 to 63/64 (the one remaining failure being the pre-existing Synapse-flaky scenario Phase 22 already identified as not-an-axon-bug). **A full `tests/csapi` package re-run was attempted but hit the harness's own suggested `-timeout 600s`** (this repo's own README-documented command) before finishing all ~64 tests' container-per-test overhead — a tooling/time-budget limit, not a test result, so it isn't being reported as a confirmed whole-package number this phase. Re-running the full package with a longer timeout (or via `-run` batches) is the natural next-step verification, same caveat Phase 23 left for its own unverified-against-Complement fixes.

Full `mix test`: **1,256 tests + 15 doctests, 0 failures**, confirmed on two consecutive solo runs (a first solo run right after the Complement rebuild hit 1 stray failure — the already-documented `AxonPush.Dispatcher`-vs-Ecto-sandbox-teardown race from Phase 22's "test-suite-not-concurrency-safe" note, not reproducible on a clean rerun and not related to this phase's changes).

## Phase 25 — soft_failed's own read side had a write-side gap: it could be un-set

A live Complement run targeting the auth-chain/rejection cluster (`TestInboundFederationRejectsEventsWithRejectedAuthEvents`, `TestCorruptedAuthChain`, `TestUnrejectRejectedEvents`) confirmed two of the three now pass, closing out exactly what commit `9088b9e` (`soft_failed`, the second/current-state auth check) and `61070db` (filtering rejected/soft-failed out of the client-facing `GET /event/{id}`) set out to fix:

- `TestInboundFederationRejectsEventsWithRejectedAuthEvents` — pass.
- `TestUnrejectRejectedEvents` — pass.
- `TestCorruptedAuthChain` — still fails, but not on its own assertions: it fails at `MustJoinRoom`'s `send_join`, `403 M_FORBIDDEN: Bad event signature`, before the auth-chain-corruption scenario it actually tests ever gets to run. This is the same `send_join` signature mismatch Phases 19/21/22 each independently verified, byte-for-byte across three different verifiers (including `gomatrixserverlib`'s own), is not an axon bug. Re-examined a fourth time this phase rather than trusted at face value: no code in `apps/axon_crypto/` (redaction, hashing, canonical JSON — the layer the prior investigations pinned the mismatch to being upstream of) has changed since the Phase 19/21/22 investigations, and the check that fails is `verify_event_signature/2` on the inbound PDU, called *before* any of this phase's own `soft_failed`/`RoomProcess` code ever runs — there is no plausible interaction between the two. `TestCorruptedAuthChain` also targets a specific Synapse regression shape ("sort events topologically, auth-check against an in-memory event map, don't evict a dropped entry from that map") that doesn't structurally exist in axon regardless of `send_join`: every auth/ancestry check axon does (`EventStore.any_rejected?/1`, `unknown_ids/1`, `AxonFederation.Backfill.apply_oldest_first/3`'s per-event `RoomProcess.apply_remote_event/2` calls) reads Postgres directly, with no in-memory event cache carried across a batch to go stale.

While re-reading `9088b9e`'s diff end-to-end to confirm the above (per this phase's own instructions — read the fix before trusting the "known gap" label), found a real gap in the fix itself, unrelated to any of the three tests above: `EventStore.insert_event/2`'s `on_conflict` clause — added for the legitimate, already-tested "unreject" case (a previously-rejected event that gets accepted on a later attempt, `TestUnrejectRejectedEvents`'s exact shape) — unconditionally reset **both** `rejected` and `soft_failed` to `false` on any conflict. But `9088b9e`'s own doc comment for `insert_soft_failed_event/2` promises the opposite for soft-failure specifically: "unlike rejection, there is no un-soft-fail... matching Synapse's own one-time-determination behavior." Nothing enforced that promise. `AxonRoom.RoomProcess.apply_remote_event/2` has no short-circuit for an event_id it already soft-failed — it re-runs the full resolution/auth pipeline on every call, by design, so that a genuinely-retried *rejected* event can be unrejected. A retried or backfill-refetched PDU that had previously been soft-failed, arriving after room state moved on enough that it would now legitimately pass `AuthRules.check` against the new `state.current_state` (the ban that soft-failed it gets reversed, the sender rejoins, etc. — ordinary, expected DAG churn, not an attack), would silently pass straight through: `EventStore.insert_event/2`'s conflict clause would flip `soft_failed` back to `false` and re-run the state/membership writes, and `RoomProcess` would advance the room head and broadcast it to `/sync` — resurrecting an event the spec says must stay buried for good. `insert_event/2` also isn't `RoomProcess`-exclusive — `AxonFederation.RoomJoin`/`RoomKnock`/`RoomLeave` and `AxonWeb.FederationController` all call it directly too, so the fix had to hold at the persistence layer, not just one call site.

Fixed two ways, matching the two ways the gap was reachable:

- `EventStore.insert_event/2` now checks for an already-soft-failed row *before* doing anything else, and if found, returns it unchanged — no flag flip, no state/membership/auth-edge writes, no stream_ordering bump. Covers every caller, not just `RoomProcess`.
- `AxonRoom.RoomProcess.apply_remote_event/2` also gained its own guard, checked first (before the existing `any_rejected?`/`unknown_ids` transitive-rejection checks): an inbound PDU whose event_id is already stored soft-failed replies `{:error, :soft_failed}` immediately, without re-running `AuthRules.check`/`StateResolver` at all. Belt-and-suspenders with the `EventStore` fix, and the one that actually matters for the common case (a retried/backfilled `/send` PDU) — it also avoids the wasted resolution work the old code would have done only to have its result silently overridden.

New regression coverage: three tests in `event_store_test.exs` ("insert_event/2 — never un-soft-fails" — a resent soft-failed state event stays soft-failed and unapplied, a resent soft-failed membership event still derives no `room_memberships` row, no `stream_ordering` bump) and one in `room_process_test.exs` (the full scenario: join, fork, ban → soft-fail a message, unban and rejoin so the identical PDU would now legitimately pass, resend it, assert it's still `{:error, :soft_failed}`, never becomes the room head, never appears in `/sync`).

Full `mix test`: 0 failures (see this phase's commit for the exact count).

## Phase 26 — Two new endpoints, and the Complement ground-truth pass behind Phases 24 and 25

Two pieces of work landed together this phase: two features that were simply absent from the codebase before (`/threads` and refresh tokens), and a scoped, real Complement run whose findings are what actually drove Phase 24's and Phase 25's fixes. That run is documented here rather than folded silently into those two phases' own entries, because it covered substantially more ground than either write-up shows on its own — thirteen ROADMAP-cited test names plus four more resolved out of two of the vaguer bullets — and because several of its results correct claims elsewhere in this document and in the README that the run itself disproved.

### `GET /_matrix/client/v1/rooms/{roomId}/threads` (`f3a4a68`, stable spec, formerly MSC3856)

Paginated thread-root listing, most-recently-active-thread-first — "active" meaning the latest reply's `stream_ordering`, not the root event's own position, which is why it needed its own query rather than reusing `get_messages/4`'s plain timeline order. `EventStore.get_thread_roots/3` groups `m.thread`-related child events by their `m.relates_to.event_id`, filters out rejected/soft-failed children, and orders each group by `max(stream_ordering)`; `include=participated` restricts to threads where the requesting user sent at least one child event, reusing the exact "participated" notion `bundle_relations/3` already computes for `unsigned.m.relations.m.thread.current_user_participated`, so the list endpoint and the per-event relation bundle can't drift apart on what "participated" means. `EventController.get_threads/2` is membership-gated and keyset-paginated (`from`/`limit`/`next_batch`) the same way `get_relations`/`get_messages` already are. 7 new tests in `threads_test.exs`: root listing and activity ordering, `include=all` (default) vs `include=participated` actually filtering, invalid `include` rejected, non-member 403, empty-room chunk (not an error), multi-page pagination with no repeats or gaps.

### Refresh tokens (`2da5603`, `a682590`; stable Matrix spec, formerly MSC2918)

`POST /login` and `/register` now accept `refresh_token: true`, opting the client into a paired `refresh_token` and a real `access_token` expiry (`expires_in_ms`, default 5 minutes — matching Synapse's `refreshable_access_token_lifetime` default). A request that doesn't opt in is unaffected: a single never-expiring `access_token`, exactly the pre-existing behavior. New `POST /_matrix/client/v3/refresh` rotates a `refresh_token` for a fresh access/refresh pair — single-use, with the old token invalidated atomically as part of the same rotation so a concurrent replay can't both succeed — carrying the same `user_id`/`device_id` forward. Error codes were checked against Synapse's own `auth.py` refresh-token handling rather than guessed: an unrecognized token is `401 M_UNKNOWN_TOKEN`, an already-used or expired one is `403 M_FORBIDDEN`. Storage rides the `refresh_tokens` table that has existed, unused, since the initial auth migration, plus a new `access_tokens.expires_at_ms` column; `logout`/`logout_all` now also drop the affected device's/user's refresh tokens, so a live `refresh_token` can't outlive the `access_token` it was issued alongside. 11 new tests in `refresh_token_test.exs`.

### The Complement ground-truth pass

Ran each of ROADMAP's most-cited "known gap" test names individually against a freshly built `axon-complement:latest` image (the README-documented full build), plus four extra names resolved out of two of the vaguer bullets — "MSC4297 state-res v2.1 (2 tests)" and "device-list/to-device over federation timeouts" — by grepping Complement's own source for the test functions actually behind them. 20 tests total, split across the `tests/csapi` and `tests/` packages each one actually lives in, each run individually with its own hard wall-clock kill so one stuck test couldn't stall the batch. No axon `.ex` file was touched before or during the run — this is what "known gap" looks like against current `master`, not an inference from reading code.

**Confirmed already passing, no code change needed** — closing out the hedging language Phase 20 left around several of these ("not fully triaged", "not root-caused") even though its own final tally already counted them as passing: `TestAsyncUpload` (async media upload, MSC2246 — fixed as a side effect of Phase 20's stuck-connection bug), `TestE2EKeyBackupReplaceRoomKeyRules` (Phase 20's key-backup replace-rule fix, spec 11.12.3.3), `TestServerNotices` (Phase 20's bot-mxid and room-reuse fixes), `TestSearch` (Phase 20's pagination/ordering fixes), and `TestDeviceListUpdates`/`TestPresence` (both `csapi`). All six ran clean. This is the first time since Phase 20 itself that any of them were re-run individually against a live image rather than taken on the strength of that one historical number.

**Confirmed still genuinely open, with the root cause narrowed further than before:**

- `TestCorruptedAuthChain` — still `send_join => 403 M_FORBIDDEN: Bad event signature`, the same dev-dependency-pinned mismatch Phases 19/21/22/25 each independently confirmed is not axon's bug. A fifth data point, not a fifth investigation.
- `TestNetworkPartitionOrdering` — still fails, and **Phase 23's depth-based `dir=b` ordering fix (`0d3a448`) did not close it**: backward pagination now returns a wrong event at the second position (event 3's id where event 2's was expected) — a narrower symptom than before the fix, but still a real topological-ordering bug. Phase 23 explicitly flagged this fix as "expected to close... unconfirmed against live Complement"; it's now confirmed not fully closed.
- `TestInboundCanReturnMissingEvents` — still fails, and **Phase 23's `guest_access` positional fix (`23223ae`) did not close it either**, though it changed the failure entirely: all 4 visibility subtests now fail on `get_missing_events` gap-filling returning an `m.room.history_visibility` event with no `msgtype` in its content — a different symptom than the event-sequence shift the guest_access fix targeted. `23223ae` remains a real, independently-correct fix (Synapse's own preset table confirms the value it corrected); it just wasn't this test's only blocker.
- `TestJumpToDateEndpoint` — still fails on 4 federation subtests (wrong event id returned for a given timestamp). Phase 23's local-history-distrust heuristic (`55e4f77`) itself flagged a full flip as not guaranteed on that fix alone; confirmed here that the federation subtests are still genuinely wrong, not merely unconfirmed.
- `TestMSC4297StateResolutionV2_1_starts_from_empty_set` / `..._includes_conflicted_subgraph` — both fail before axon's own state-res code ever runs, inside Complement's pinned `gomatrixserverlib` snapshot's `eventauth` package (`failed to sign event: eventauth: invalid ID` on a v12 room ID). This is the exact error class Phase 22 flagged as a hypothesis for a *different* test (`TestMSC4291RoomIDAsHashOfCreateEvent_AuthEventsOmitsCreateEvent`, "smells like the same class of dev-dependency issue as send_join"); this run gives that hypothesis a second, independent data point with the literal error message rather than a resemblance.
- `TestDeviceListsUpdateOverFederation` / `TestToDeviceMessagesOverFederation` — still time out (`MustSyncUntil`, ~30-50s) specifically on the *initial invite* / *initial to-device* delivery path. Their sibling tests — `TestDeviceListsUpdateOverFederationOnRoomJoin` and `TestUserAppearsInChangedDeviceListOnJoinOverFederation` — both pass, narrowing the "device-list/to-device over federation timeouts" bullet (open, unscoped, since Phase 17) down to the invite-path specifically rather than the feature as a whole.

**Turned out to have real, narrow gaps and got fixed this phase** — covered in full in their own entries rather than repeated here: the point-in-time state gaps (`TestGetRoomMembersAtPoint`, `TestLeftRoomFixture` — see **Phase 24**) and `soft_failed`'s write-side un-set bug found while re-reading its own diff rather than trusting the "known gap" label (see **Phase 25**). Both phases' fixes and regression coverage are already committed; this entry exists to record that the same Complement run behind both of them also covered thirteen other names, and most of them needed nothing.

Full `mix test`, solo run: **1,260 tests + 15 doctests, 0 failures** — unchanged from Phase 25's own count, since this phase's two new features and the verify pass together add no regressions and, for the verify pass specifically, no production code at all.

## Phase 27 — Four Phase 23 residuals actually closed this time (each live-Complement-confirmed), plus a real identity-server-mediated 3pid invite pipeline

Phase 26's ground-truth run found that three of Phase 23's `mix test`-only fixes hadn't actually closed their target tests, and separately narrowed the long-open "device-list/to-device over federation timeouts" bullet down to the initial-invite path specifically. This phase root-caused and fixed all four, **each verified this time via a live rebuilt-`axon-complement:latest` run against the specific target test, not mix test alone** — the same lesson Phase 22 already learned about "should be fixed" fixes not always panning out against real `gomatrixserverlib` traffic. It also replaced axon's always-self-signed 3pid-invite shim with a real Matrix identity-service-api integration, backed by a real Sydent instance.

### `TestNetworkPartitionOrdering` — a `prev_batch` off-by-one, one layer up from where Phase 23 looked

Phase 23's depth-first `dir=b` ordering fix (`0d3a448`) was real and independently correct, but Phase 26 found it narrowed the failure rather than closing it: backward pagination returned event 3's id where event 2's was expected. The actual remaining bug (`3c696ca`) was one layer up, in `SyncController`'s own `prev_batch` computation: all three branches (initial sync, newly-joined-room sync, incremental sync) computed `hd(tl_events).stream_ordering - 1`, but `EventStore.get_messages/4`'s `dir=b` bound (`stream_ordering < from_ordering`) is already exclusive of `from_ordering` itself — subtracting 1 first over-excluded by one event, silently dropping the room event immediately preceding the sync response's timeline window off the very next backward `/messages` page. Two sibling branches in the same functions already used the bare (no `-1`) form for the same purpose, corroborating this was a genuine bug and not an intentional convention. Reproduced directly first (a temporary instrumented test driving `RoomProcess` exactly like the Complement scenario, confirmed fully removed afterward) before writing the real fix, rather than guessing from the symptom alone.

Fixed all three occurrences, added a real HTTP-level regression test (`event_controller_test.exs`). **Live Complement, rebuilt image: `TestNetworkPartitionOrdering` PASS (22.69s)** — previously failing on `/messages?dir=b` returning the wrong event id.

Flagged but deliberately not fixed: while reproducing the scenario, also found that `RoomProcess.apply_and_advance/3` unconditionally overwrites `state.depth`/`state.last_event_id` with whatever event it just applied, even when that event's own depth is *lower* than the room's already-known head — a forked/late-arriving remote event can regress the tracked head, which matters for any future fork/merge-heavy federation scenario past the two-branch case this codebase's local-chain-building model assumes. Not this test's blocker (the window `/messages` computes here excludes the tied events it would produce), so left as a known gap rather than fixed under time pressure.

### `TestInboundCanReturnMissingEvents` — three real bugs, not the redaction gap Phase 26 guessed at

Phase 26 found this failing on a different symptom than Phase 23's `guest_access` fix targeted: all 4 visibility subtests failing on `get_missing_events` returning an `m.room.history_visibility` event missing `msgtype`, and guessed it might be an over-eager redaction rule. It wasn't — it was three distinct, unrelated bugs (`8d20b4a`):

1. `walk_missing_events` seeded its queue with `latest_events` themselves rather than their `prev_events` (per spec, the walk starts one level back) — every response carried an extra, already-known trailing event the requester never asked for, which alone broke the `world_readable` subtest's index-based assertions.
2. `get_missing_events` applied no `history_visibility` filtering at all — every event came back with full content regardless of whether the requesting origin server had a member in the room at that point in history. New `origin_visibility_bounds/2` reuses `EventController`'s existing `visibility_bounds`/`event_visible?` machinery, judged against the union of the origin server's own room members rather than a single local user. Out-of-scope events now come back redacted via `AxonCrypto.Redaction`, never omitted (omitting would break `prev_events` continuity for the gap-filling server) — this is the actual source of the "missing `msgtype`" symptom Phase 26 saw.
3. `RoomProcess.send_event` had no deduplication for state events — `PUT`ting `history_visibility=shared` onto a room whose `public_chat` preset already set it to `shared` minted a second, content-identical event, throwing off the `shared` subtest's own index-based assertions. New `duplicate_state_event/3` mirrors Synapse's own `deduplicate_state_event` (confirmed against its source): same sender + same content as current state for that `(type, state_key)` returns the existing event_id instead of persisting a new one.

**Live Complement, rebuilt image: all 4 `TestInboundCanReturnMissingEvents` subtests (`world_readable`, `shared`, `invited`, `joined`) PASS.** Confirmed no regression in `TestGetMissingEventsGapFilling` and `TestOutboundFederationEventSizeGetMissingEvents` (same file, both still pass). `TestOutboundFederationIgnoresMissingEventWithBadJSONForRoomVersion6` independently confirmed already failing on master before this change (the separate, still-open backfill-fallback gap from Phase 22) — not a regression.

### `TestJumpToDateEndpoint` — the trust heuristic needed to know its own query direction

Phase 23's local-history-distrust heuristic (`55e4f77`) compared a candidate answer only against this server's single earliest known event, regardless of query direction. That misses the actual shape a late-joining server's local history takes: a remote join (`AxonFederation.RoomJoin.import_room_state/5`) only imports a state snapshot, never the intervening timeline — so the real knowledge gap sits in the *middle* of local history, not necessarily at either edge. A `dir=b` query landing on an early-but-fully-connected state event (an earlier-joined roommate's own membership event, exactly Complement's `remoteCharlie` scenario) is neither this server's earliest nor latest known event, so the old heuristic waved it through as trustworthy and federation was never consulted.

Fixed (`a59560a`) with `trustworthy_local_timestamp_answer?/3` now also taking `dir` and checking for a real DAG discontinuity next to the candidate via its `prev_event_ids`, matching Synapse's `is_next_to_backward/forward_gap` concept: `dir=f` distrusts on "earliest known" (unchanged) OR a backward gap; `dir=b` distrusts on a forward gap, which subsumes the old "latest known" case too. 7 new direct `event_store_test.exs` cases, including a "backfilled in later, now trustworthy" control case.

**Live Complement, rebuilt image: the two explicitly-in-scope federation subtests — "looking forwards" and "looking backwards, should be able to find event that was sent before we joined" — now PASS**, plus the appservice-`?ts=`-dependent "before the room was created" subtest flipped PASS as a bonus. **Two "can paginate backwards (start/end)" subtests still FAIL**, but on a newly-identified, genuinely separate root cause: `GET /context` computes its before/after pagination boundary from the target event's raw `stream_ordering`, assuming `stream_ordering` tracks depth — an assumption a federation-backfilled event breaks, since it's inserted (and stream-ordered) well after chronologically-later events the server already had. Left as a follow-up rather than risk regressing `3c696ca`'s just-fixed `dir=b` pagination-window invariants under time pressure — the trust-heuristic fix itself is confirmed working for both of the tests it explicitly targeted.

### `TestDeviceListsUpdateOverFederation` / `TestToDeviceMessagesOverFederation` — four compounding bugs, each only reachable once the last was fixed

Phase 26 narrowed these to the *initial* invite/to-device delivery path specifically (the room-join/device-list-visibility siblings already passed). Root cause was a chain of four bugs (`7ecc5cf`), each discovered only after fixing the one before it made the next one visible in a live rebuild+run:

1. `AxonRoom.CreateRoom.execute/2`'s own `invite` list sent every invitee — remote ones included — through a local-only `RoomProcess.send_event/5` path whose generic PDU fan-out only notifies servers of already-*joined* members, so a brand-new remote invitee's server never heard about the invite at all. This alone explained both tests' `MustSyncUntil` timeouts. Fixed by having `createRoom`'s invite loop only handle local invitees, with `RoomController.create/2` routing remote ones through the same federation `/invite` v2 handshake the standalone `POST /rooms/:id/invite` endpoint already used.
2. Once invites started delivering, `room_controller.ex`'s `join/2` and `do_knock/3` used `EventStore.room_exists?/1` ("any row in `rooms` at all") to decide local-vs-federated join — a bare invite stub also satisfies that, so accepting a federated invite fabricated a fake local-only join nobody else in the real room (least of all the inviting server) ever heard about. Fixed by reusing `leave/2`'s existing, correct `resident_room?/1` check (real `m.room.create` present) for join/knock too.
3. With join fixed, a third bug surfaced: `AxonFederation.DeviceListFanout`'s scan cursor lived only in memory and always restarted at 0 — indistinguishable from a fresh boot at the Elixir level — so a container restart (exactly what the `interrupted_connectivity`/`stopped_server` subtests do) re-fanned every historical device-list change as if new, producing spurious duplicate `device_lists.changed` entries. Fixed by persisting the cursor (new `device_list_fanout_cursor` table), reloaded on restart instead of defaulting to 0.
4. A real off-by-one in `AxonFederation.OutboundQueue.reschedule/2`: the backoff exponent used the post-increment attempt count directly, doubling every delay one step too early. Fixed, and lowered the base backoff from 30s to 5s so a brief simulated outage's retry lands well inside Complement's ~30-50s sync-timeout budgets.

**Live Complement, rebuilt image: `TestDeviceListsUpdateOverFederation` PASS (72.50s)** — `good_connectivity` (3.70s), `interrupted_connectivity` (12.38s), `stopped_server` (22.51s), all green. **`TestToDeviceMessagesOverFederation` PASS (89.94s)** — same three subtests, all green. No regression in the two siblings, re-run against the same image: `TestDeviceListsUpdateOverFederationOnRoomJoin` PASS (19.93s), `TestUserAppearsInChangedDeviceListOnJoinOverFederation` PASS (34.96s).

### A real identity server, and real identity-server-mediated 3pid invites end to end

Deployed [Sydent](https://github.com/matrix-org/sydent) (matrix.org's reference identity server) via a new `docker-compose.identity.yml` (`879d77a`) — the official `matrixdotorg/sydent:latest` image, persistent volume, documented in README's new "Identity server (3pid invites)" section — establishing the `DEFAULT_IDENTITY_SERVER` env var contract (unset by default; a plain base-URL swap, nothing Sydent-specific baked into the contract itself).

Then (`7bf65a4`) replaced the always-self-signed 3pid-invite shim with real identity-service-api integration: `RoomController.invite_3pid/4` resolves `id_server`/`id_access_token` per spec, falling back to `DEFAULT_IDENTITY_SERVER` with a self-registered OpenID token (new `AxonWeb.OpenidTokens` plus real `POST /_matrix/client/v3/user/:user_id/openid/request_token` and `GET /_matrix/federation/v1/openid/userinfo` endpoints) when the client supplies neither; does a real privacy-preserving hashed lookup (`hash_details`+`lookup`) to send a direct invite when the 3pid is already bound; and for an unbound email 3pid calls the identity server's real store-invite, so the `m.room.third_party_invite` event carries the identity server's own `public_key`/`key_validity_url`/`display_name`/`token` instead of axon's own key. `createRoom`'s `invite_3pid` list goes through the same path. Join-time, `AuthRules.third_party_signature_valid?/2` was fixed to verify whatever fields a real identity server actually signed — discovered live: Sydent's own signing servlet includes an extra `sender` field the old hardcoded mxid/token allowlist silently failed to verify — and a new cached `AxonWeb.IdentityServer.pubkey_valid?/2` (called from `RoomController`'s join handler, deliberately kept out of `AuthRules` itself to keep it free of network I/O on federation/state-resolution replay paths) confirms the identity server hasn't revoked the signing key before honoring a `third_party_signed` proof. Also discovered live: a real identity server's `key_validity_url` can be a *relative* path (Sydent returns bare paths like `/_matrix/identity/api/v1/pubkey/isvalid`) — not documented anywhere found by reading the spec alone — so `RoomController` now resolves it against the identity server's own base URL before storing it in room state.

msisdn has no store-invite equivalent per spec (confirmed against the actual spec text, not a paraphrase — delegation is email-only), so axon now makes a real best-effort `validate/msisdn/requestToken` call (verified live against Sydent: accepted, returns a real `sid`, can't complete an OTP send since Sydent has no SMS provider configured, exactly as expected) and falls back to axon's own self-signed content for the state event itself — the same shape the old shim always produced for every medium, still fully functional for a same-server test flow, just without external identity-server delegation of msisdn *delivery*, since spec doesn't offer one to delegate to.

**Live proof, not mocked**: since Complement has no identity-server test coverage at all (it's a homeserver-to-homeserver federation suite), the live-verification requirement for this feature was met with a real end-to-end run against a real, freshly-configured Sydent container instead — captured in `apps/axon_web/test/e2e/identity_server_3pid_test.exs`, which passes: real OpenID self-registration (Sydent calling back over real TLS to axon's own `GET /_matrix/federation/v1/openid/userinfo`, requiring a real Bandit HTTPS listener with a throwaway self-signed test cert), real `hash_details`+`lookup`, real store-invite with its relative `key_validity_url` resolved, real `requestToken`/`submitToken`/`bind` (including a real captured SMTP message, parsed the way a person reading their inbox would), and the resulting real signed proof verified successfully by axon's own `AuthRules` code through a real join. New `AxonWeb.FakeIdentityServer` (matching the existing `FakeRemoteMatrixServer`/`FakePusherGateway`/`FakeSmtpServer` convention) covers edge cases a real identity server can't conveniently be made to produce on demand, used in the rewritten `third_party_invite_test.exs`.

The real Sydent container needs three one-time `sydent.conf` hand-edits with no environment-variable equivalent (`ip.whitelist` for loopback — Sydent SSRF-blocks `127.0.0.1`/`::1` by default; `federation.verifycerts=False` to accept axon's self-signed test cert; `templates.path` fixed from a broken relative default that otherwise crashes store-invite's real email send) plus `network_mode: host` on the compose service — all documented in README and the e2e test's own moduledoc, with exact reproduction commands. Without them, `identity_server_3pid_test.exs` self-skips (logged, not a failure) rather than going red; the rest of the suite doesn't need any of this.

### Where this leaves the federation package

All four residual fixes and the identity-server work landed as six separate commits (`3c696ca`, `8d20b4a`, `a59560a`, `7ecc5cf`, `879d77a`, `7bf65a4`), each with its own regression tests. Net movement against named Complement tests this phase, each individually confirmed live: `TestNetworkPartitionOrdering` fail → pass. `TestInboundCanReturnMissingEvents` fail → pass. `TestDeviceListsUpdateOverFederation` timeout → pass. `TestToDeviceMessagesOverFederation` timeout → pass. `TestJumpToDateEndpoint` remains fail overall — 3 of 5 federation subtests now pass (both explicitly-in-scope ones plus a bonus), but the test as a whole still fails on the 2 pagination subtests' newly-identified, separate `GET /context` root cause. No full before/after package-wide diff was run this phase (each fix was verified by running its own specific target test(s) individually against a freshly rebuilt image, not a whole-package re-run) — that remains the natural next step for a fresh aggregate number, same caveat prior phases have left.

Full `mix test`, solo run: **1 doctest, 645 tests, 0 failures** — the final count for this phase, confirmed clean on the last run before this writeup (an intermediate run mid-phase, before the `OutboundQueue`/`DeviceListFanout` fixes were finished, showed 3 unrelated `Phase9EphemeralTest` flakes under full-suite load; non-reproducible in isolation and gone on the very next full run, consistent with the same pre-existing timing-sensitive flake prior phases have already documented).
